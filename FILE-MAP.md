# CC Immune — File Map

Auto-generated by `scripts/generate-map.js`. 156 source files,
33,615 lines.

Each entry is the file's own `@description` header, so this map cannot drift
from the code without the header drifting too.


## `src/`

| File | Lines | Purpose |
|---|---:|---|
| `ImmuneClient.js` | 245 | The Discord client, extended with the DI container and registry. |

## `src/api/dashboard/`

| File | Lines | Purpose |
|---|---:|---|
| `DashboardAuth.js` | 350 | Session + login handling for the web dashboard. |
| `DashboardService.js` | 453 | Data + permission layer for the web dashboard. |
| `page.js` | 597 | Server-rendered shell for the web dashboard. |
| `router.js` | 300 | HTTP routes for the web dashboard. |

## `src/api/`

| File | Lines | Purpose |
|---|---:|---|
| `server.js` | 406 | Observability HTTP server (§10 "Observability"). |

## `src/api/site/`

| File | Lines | Purpose |
|---|---:|---|
| `content.js` | 314 | Bilingual copy for the public landing page. |
| `page.js` | 398 | Server-rendered landing page (§12 "Website"). |
| `styles.js` | 197 | Landing-page CSS, generated from the shared theme palette. |

## `src/commands/ai/`

| File | Lines | Purpose |
|---|---:|---|
| `coach.js` | 215 | AI Coach commands (§7). |
| `memory.js` | 179 | User-facing AI memory controls (§7.5.5). |
| `translate.js` | 91 | AI translation, EN <-> AR focused (§8). |

## `src/commands/config/`

| File | Lines | Purpose |
|---|---:|---|
| `config.js` | 150 | Per-guild module toggles (§3.6.5 guild admin surface). |
| `dashboard.js` | 156 | Issues a private login link for the web dashboard (§9). |
| `language.js` | 95 | Language switching (§8). |
| `prefix.js` | 155 | Per-guild prefix management (§3.5). |
| `reactionrole.js` | 172 | Button-based reaction role panels (§7). |
| `setup.js` | 344 | Server setup: auto-layout, status dashboard, config export (§9). |
| `suggest.js` | 102 | Community suggestions with voting (§7). |
| `verify.js` | 92 | Verification panel and gate (§7). |

## `src/commands/developer/`

| File | Lines | Purpose |
|---|---:|---|
| `doctor.js` | 363 | Live diagnostics from inside Discord (§10.5.4). |
| `module.js` | 347 | Owner-level module control (§3.6.5). |

## `src/commands/economy/`

| File | Lines | Purpose |
|---|---:|---|
| `economy.js` | 173 | Currency commands (§7). |
| `shop.js` | 210 | Server shop and inventory (§7). |

## `src/commands/giveaways/`

| File | Lines | Purpose |
|---|---:|---|
| `giveaway.js` | 183 | Giveaway management (§7). |

## `src/commands/leveling/`

| File | Lines | Purpose |
|---|---:|---|
| `leaderboard.js` | 61 | Paginated XP leaderboard. |
| `rank.js` | 69 | XP rank card and leaderboard. |

## `src/commands/mlbb/`

| File | Lines | Purpose |
|---|---:|---|
| `draft.js` | 185 | Draft analysis and ban suggestions (§MLBB systems). |
| `hero.js` | 321 | Hero database commands (§MLBB systems). |
| `item.js` | 228 | Item and build reference (§MLBB systems). |
| `meta.js` | 130 | Meta tier list (§MLBB systems). |
| `news.js` | 96 | Patch notes and MLBB news subscriptions (§MLBB systems). |
| `profile.js` | 245 | MLBB player profiles and rank tracking (§MLBB systems). |
| `scrim.js` | 183 | Scrim finder / LFT board (§MLBB systems). |
| `tournament.js` | 271 | Tournament management (§MLBB systems). |

## `src/commands/moderation/`

| File | Lines | Purpose |
|---|---:|---|
| `moderation.js` | 241 | Core moderation actions (§7 community systems). |

## `src/commands/music/`

| File | Lines | Purpose |
|---|---:|---|
| `music.js` | 237 | Playback controls (§6). |
| `play.js` | 133 | Music playback from any supported link or search (§6). |
| `prayer.js` | 79 | Prayer times (§6 bonus). |
| `quran.js` | 241 | إذاعة القرآن الكريم من القاهرة (§6). |

## `src/commands/tickets/`

| File | Lines | Purpose |
|---|---:|---|
| `ticket.js` | 204 | Ticket commands and panel setup (§7). |

## `src/commands/utility/`

| File | Lines | Purpose |
|---|---:|---|
| `about.js` | 76 | Bot info, links and the Moonton disclaimer. |
| `announce.js` | 76 | Formatted announcements (§7). |
| `help.js` | 202 | Paginated help that adapts to the invocation mode (§3.5). |
| `ping.js` | 67 | Latency check. |
| `poll.js` | 79 | Button-based polls (§7). |

## `src/components/buttons/`

| File | Lines | Purpose |
|---|---:|---|
| `audio.js` | 276 | Live audio control panel — music and Quran radio in one place. |
| `confirm.js` | 35 | Generic confirm/cancel buttons. |
| `giveaway.js` | 62 | Handles the giveaway entry button. |
| `hero.js` | 373 | Interactive navigation for hero embeds. |
| `panel.js` | 111 | Interactions for the auto-updating info panels. |
| `poll.js` | 79 | Poll voting. |
| `rr.js` | 82 | Reaction-role buttons. |
| `setup.js` | 143 | Buttons for the interactive setup wizard (`setup:*`). |
| `suggestion.js` | 70 | Suggestion voting buttons. |
| `ticket.js` | 84 | Handles every `ticket:*` button. |
| `verify.js` | 71 | Verification button. |

## `src/components/selects/`

| File | Lines | Purpose |
|---|---:|---|
| `setup.js` | 110 | Select menus for the interactive setup wizard (`setup:*`). |

## `src/config/`

| File | Lines | Purpose |
|---|---:|---|
| `constants.js` | 346 | Project-wide enums, limits, regexes and the component-ID contract. |
| `env.js` | 467 | Zod-validated environment schema. |
| `features.js` | 129 | Level-1 (build/env) module gates — see §3.6.2. |
| `intents.js` | 140 | Gateway intent + partial resolution. |
| `modules.js` | 519 | The module manifest (§3.6.1). |
| `theme.js` | 307 | Single source of truth for all visual branding. |

## `src/`

| File | Lines | Purpose |
|---|---:|---|
| `container.js` | 387 | Dependency injection container (§CODE STYLE "use DI"). |

## `src/core/`

| File | Lines | Purpose |
|---|---:|---|
| `ArgumentParser.js` | 690 | Turns raw prefix text into the exact `ctx.args` object Discord produces for a slash command (§3.5 "Argument parsing parity"). |
| `BaseCommand.js` | 231 | Command contract for BOTH slash and prefix invocation (§3.5). |
| `BaseComponent.js` | 109 | Contract for buttons, select menus and modals. |
| `BaseEvent.js` | 82 | Event contract (§0 consistency contracts). |
| `BaseService.js` | 150 | Base class for every service (§5). |
| `CommandContext.js` | 328 | The unified command context (§3.5) — the heart of dual-mode. |
| `ErrorBoundary.js` | 397 | The single guard that wraps every async handler (§10.5.5). |
| `Logger.js` | 398 | Professional logging facade (§10 "Logger"). |
| `Middleware.js` | 543 | The shared guard chain for BOTH entry paths (§3.5, §3.6.4). |
| `PrefixContext.js` | 350 | CommandContext backed by a Message (§3.5). |
| `Registry.js` | 420 | Generic recursive auto-loader (§3.6.4, §CODE STYLE). |
| `SlashContext.js` | 260 | CommandContext backed by a ChatInputCommandInteraction (§3.5). |

## `src/core/errors/`

| File | Lines | Purpose |
|---|---:|---|
| `ImmuneError.js` | 415 | Typed error hierarchy (§10.5.1). |
| `codex.js` | 702 | The Error Codex (§10.5.2) — machine-readable troubleshooting KB. |

## `src/database/`

| File | Lines | Purpose |
|---|---:|---|
| `connection.js` | 350 | Mongoose connection manager (§10 reliability, §10.5.6 self-healing). |

## `src/database/repositories/`

| File | Lines | Purpose |
|---|---:|---|
| `BaseRepository.js` | 298 | Shared repository behaviour (§3 "no raw model calls in commands"). |
| `GuildRepository.js` | 172 | Guild config access with hot-path caching (§3.5 performance). |
| `MemberRepository.js` | 135 | Guild member state: XP, levels, leaderboards. |
| `UserRepository.js` | 91 | Global user profile access. |
| `index.js` | 24 | Repository registration for the DI container. |

## `src/diagnostics/`

| File | Lines | Purpose |
|---|---:|---|
| `FeatureRegistry.js` | 546 | THE module state authority (§3.6.3). |

## `src/events/client/`

| File | Lines | Purpose |
|---|---:|---|
| `error.js` | 25 | Gateway error handling (§10 "Reconnection handling"). |
| `ready.js` | 31 | Fires once the gateway handshake completes. |

## `src/events/guild/`

| File | Lines | Purpose |
|---|---:|---|
| `guildCreate.js` | 29 | Registers a guild when the bot is added. |
| `guildDelete.js` | 28 | Soft-deletes a guild so its settings survive a re-invite. |
| `guildMemberAdd.js` | 26 | Welcome + auto-role on join. |
| `guildMemberRemove.js` | 30 | Goodbye message + role snapshot for rejoin restoration. |
| `messageCreate.js` | 203 | Prefix command entry point + XP hook (§3.5 pipeline). |
| `messageDelete.js` | 21 | Audit log for deleted messages. |
| `messageUpdate.js` | 21 | Audit log for edited messages. |

## `src/events/interaction/`

| File | Lines | Purpose |
|---|---:|---|
| `interactionCreate.js` | 101 | Slash / component / autocomplete entry point (§3.5). |

## `src/handlers/`

| File | Lines | Purpose |
|---|---:|---|
| `AutocompleteHandler.js` | 61 | Routes autocomplete interactions to the owning command. |
| `ComponentHandler.js` | 116 | Routes buttons, select menus and modals by custom_id namespace. |
| `CooldownHandler.js` | 177 | Per-user command cooldowns (§3.5 anti-bypass). |
| `InteractionRouter.js` | 239 | Executes a command through the shared pipeline (§3.5). |
| `PrefixHandler.js` | 142 | Prefix detection for the hot message path (§3.5). |

## `src/`

| File | Lines | Purpose |
|---|---:|---|
| `index.js` | 584 | Application entrypoint. |

## `src/jobs/`

| File | Lines | Purpose |
|---|---:|---|
| `JobScheduler.js` | 229 | Cron-style background jobs (§5 SchedulerService). |

## `src/models/`

| File | Lines | Purpose |
|---|---:|---|
| `AIMemory.js` | 105 | Structured long-term AI memory facts (§7.5.7). |
| `Economy.js` | 46 | Guild-scoped currency, streaks and inventory (§4). |
| `Giveaway.js` | 54 | Giveaway state and entries (§4). |
| `Guild.js` | 297 | Per-server configuration (§4). |
| `Hero.js` | 104 | Cached MLBB hero data from the community API (§5 MLBB). |
| `Incident.js` | 90 | Deduplicated error incidents (§10.5.5). |
| `Member.js` | 79 | Guild-scoped member state: XP, levels, activity counters (§4). |
| `ModCase.js` | 48 | Unified moderation case log (§4). |
| `PlayerProfile.js` | 103 | Linked MLBB account + rank tracking (§4, MLBB systems). |
| `ReactionRole.js` | 33 | Button-based reaction role panels (§4). |
| `Scrim.js` | 44 | Scrim / practice match listings (§4). |
| `Settings.js` | 85 | Global bot singleton: maintenance, dev mode, module kill-switches. |
| `Suggestion.js` | 45 | Community suggestions with voting (§4). |
| `Ticket.js` | 52 | Support ticket state (§4). |
| `Tournament.js` | 94 | MLBB tournament brackets (§4). |
| `User.js` | 53 | Global (cross-guild) user profile (§4). |
| `_base.js` | 133 | Shared schema utilities. |

## `src/services/`

| File | Lines | Purpose |
|---|---:|---|
| `HttpClient.js` | 246 | Resilient HTTP client with circuit breaker (§2, §10.5.6). |
| `InfoPanelService.js` | 539 | Self-updating information panels (meta, top win, bans, …). |
| `LocaleService.js` | 201 | i18n with EN/AR support and RTL handling (§8). |
| `PresenceService.js` | 117 | Rotating MLBB-themed bot presence (§1 Identity & Theme). |
| `SetupPlanner.js` | 280 | Plans the server layout BEFORE touching anything (§9). |
| `SetupWizardService.js` | 290 | Stateful backend for the interactive setup wizard (§9). |

## `src/services/ai/`

| File | Lines | Purpose |
|---|---:|---|
| `AICoachService.js` | 336 | The "Immune Coach" (§7). |
| `AIGuardService.js` | 177 | Prompt-injection defence, PII scrubbing and quota (§7, §7.5.6). |
| `AIProvider.js` | 376 | Swappable AI provider factory (§7). |
| `AIUsageTracker.js` | 55 | Per-user daily token accounting (§7 cost control). |

## `src/services/ai/memory/`

| File | Lines | Purpose |
|---|---:|---|
| `MemoryService.js` | 352 | The AI memory layer (§7.5). |

## `src/services/audio/`

| File | Lines | Purpose |
|---|---:|---|
| `MusicService.js` | 658 | Lavalink v4 audio backend via Shoukaku (§6). |
| `NodeDiscovery.js` | 228 | Zero-config Lavalink discovery (§6 audio). |
| `PrayerService.js` | 119 | Prayer times via the Aladhan API (§6 bonus). |
| `QuranRadioService.js` | 236 | إذاعة القرآن الكريم من القاهرة — 24/7 Quran radio (§6). |

## `src/services/community/`

| File | Lines | Purpose |
|---|---:|---|
| `AutoModService.js` | 162 | Automatic moderation (§7). |
| `EconomyService.js` | 185 | Currency, income and transfers (§7). |
| `GiveawayService.js` | 162 | Giveaway scheduling and drawing (§7). |
| `LevelingService.js` | 120 | XP and levelling (§7 community systems). |
| `LogService.js` | 160 | Server audit logging (§7 community systems). |
| `TicketService.js` | 214 | Ticket lifecycle (§7 community systems). |
| `WelcomeService.js` | 174 | Welcome/goodbye messages and auto-roles (§7). |

## `src/services/mlbb/`

| File | Lines | Purpose |
|---|---:|---|
| `MLBBApiService.js` | 938 | Typed client for the community MLBB API (§5 MLBB). |
| `MLBBLiveClient.js` | 617 | Typed client for the community MLBB API (mlbb.rone.dev). |
| `MoontonDirectClient.js` | 409 | Talks to Moonton's own public content API, with no middleman. |

## `src/ui/`

| File | Lines | Purpose |
|---|---:|---|
| `SetupWizardUI.js` | 474 | Renders the interactive setup wizard (§9). |
| `WelcomeCard.js` | 197 | Renders the welcome banner as SVG, with no image library. |

## `src/ui/embeds/`

| File | Lines | Purpose |
|---|---:|---|
| `EmbedFactory.js` | 169 | Consistent, limit-safe embed construction (§EMBEDS). |
| `HeroEmbed.js` | 256 | Professional hero embeds with full Arabic support (§EMBEDS, §8). |
| `LiveHeroEmbed.js` | 775 | Rich, image-led hero and item embeds built from live API data. |

## `src/ui/`

| File | Lines | Purpose |
|---|---:|---|
| `links.js` | 117 | External reference links shown on hero and item embeds. |

## `src/ui/paginator/`

| File | Lines | Purpose |
|---|---:|---|
| `Paginator.js` | 114 | Generic paginated embed engine. |

## `src/ui/rows/`

| File | Lines | Purpose |
|---|---:|---|
| `index.js` | 126 | Reusable ActionRow builders. |

## `src/utils/`

| File | Lines | Purpose |
|---|---:|---|
| `localization.js` | 100 | Safe Discord command-localization helpers. |
| `redact.js` | 148 | Secret redaction for logs, error reports and diagnostic bundles. |
| `terminology.js` | 67 | Arabic game terminology lookup. |
