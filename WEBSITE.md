# 🌐 موقع البوت — دليل التشغيل

الموقع بيتقدّم من **نفس البوت**، مش محتاج استضافة تانية ولا دومين تشتريه.
بيشتغل على البورت اللي Wispbyte بيديهولك، وبيبقى لينكه:

```
http://<الآي بي بتاع النود>:<البورت>
```

---

## 1️⃣ إيه اللي في الموقع؟

| القسم | فيه إيه |
|---|---|
| **الهيدر** | لوجو البوت + تنقّل + زرار تبديل اللغة (عربي ⇄ إنجليزي) |
| **الواجهة** | عنوان متدرّج متحرّك + مؤشّر «البوت شغال دلوقتي» بيتحدّث فعلياً |
| **زراير** | ➕ أضف البوت لسيرفرك · 💬 ادخل سيرفر الدعم |
| **الأرقام** | سيرفرات · أعضاء · أوامر · أبطال · أيتمات · مدة التشغيل — **مباشرة من البوت** |
| **المميزات** | 9 كروت بكل خدمة ومعاها أمثلة الأوامر |
| **ليه CC Immune** | 4 كروت: عربي من الأساس · ما بيقعش · بياناتك بتاعتك · slash + prefix |
| **الأوامر** | جدول بأشهر 12 أمر |
| **الفوتر** | لينكات + إخلاء المسؤولية بتاع Moonton (إجباري) |

**الثيم:** أسود فضائي بتوهّج سماوي/بنفسجي، شبكة سداسية، وجزيئات طايرة —
كله بألوان `theme.js` نفسها اللي الإمبدات بتستخدمها، فالموقع والبوت شكلهم واحد.

---

## 2️⃣ التشغيل — خطوتين بس

### أ. في Wispbyte → **Startup → Variables**

```
API_SERVER_ENABLED   = true
WEBSITE_ENABLED      = true
SUPPORT_SERVER_URL   = https://discord.gg/xxxxxxx     ← لينك سيرفرك
```

> ⚠️ `API_SERVER_ENABLED` كان `false` قبل كده. **لازم تخليه `true`** عشان الموقع يشتغل.

### ب. البورت — مش محتاج تعمل حاجة ✅

Wispbyte بيبعت البورت بتاعك في متغيّر اسمه `SERVER_PORT` تلقائياً، والبوت
بياخده لوحده. **مش محتاج تكتب `API_PORT` خالص.**

في اللوج هتشوف:
```
[SUCCESS] [Api] API server listening on 0.0.0.0:25591 · website enabled at /
```

الرقم ده (`25591`) هو بورتك. افتح:
```
http://<الآي بي>:25591
```

### فين ألاقي الآي بي والبورت؟

في لوحة Wispbyte → تبويب **Network** (أو **Allocations**) هتلاقي سطر زي:
```
node-xx.wispbyte.com : 25591
```
ده اللينك بتاعك بالظبط:  `http://node-xx.wispbyte.com:25591`

---

## 3️⃣ زرار «أضف البوت» بيشتغل إزاي؟

**تلقائي.** لو ما حطّيتش `BOT_INVITE_URL`، البوت بيبنيه لوحده من `CLIENT_ID`:

```
https://discord.com/oauth2/authorize?client_id=<CLIENT_ID>&permissions=8&scope=bot%20applications.commands
```

لو عايز صلاحيات أقل من Administrator، اعمل لينك من
**Discord Developer Portal → OAuth2 → URL Generator** وحطّه في:
```
BOT_INVITE_URL = <اللينك بتاعك>
```

> 🛡️ لو أي لينك ناقص، الزرار بيبقى **رمادي ومقفول** ومكتوب عليه «قريباً» —
> ما بيوديش المستخدم على صفحة فاضية.

---

## 4️⃣ متغيّرات الموقع

| المتغيّر | الافتراضي | بيعمل إيه |
|---|---|---|
| `API_SERVER_ENABLED` | `true` | لازم `true` — الموقع بيتقدّم من نفس السيرفر |
| `WEBSITE_ENABLED` | `true` | لو `false`، `/` بيرجّع JSON بدل الصفحة |
| `WEBSITE_DEFAULT_LANG` | `ar` | اللغة اللي الصفحة بتفتح بيها: `ar` أو `en` |
| `SUPPORT_SERVER_URL` | — | لينك سيرفر الديسكورد |
| `BOT_INVITE_URL` | يتولّد تلقائي | لينك إضافة البوت |
| `WEBSITE_URL` | — | بيتحط في الـ og:url لمعاينة اللينك |
| `API_PORT` | من `SERVER_PORT` | **سيبه فاضي** على Wispbyte |

---

## 5️⃣ المسارات المتاحة

| المسار | بيرجّع إيه |
|---|---|
| `/` | صفحة الموقع |
| `/stats` | JSON بالأرقام (الصفحة بتقراه كل 30 ثانية) |
| `/health` | حالة البوت — للـ uptime monitors |
| `/metrics` | Prometheus |
| `/robots.txt` | يسمح بالأرشفة |
| `/diagnostics` | تشخيص مفصّل — **محمي** بـ `API_SECRET` |

---

## 6️⃣ خلّي البوت ما ينامش (اختياري بس مفيد)

سجّل في <https://uptimerobot.com> (مجاني) وضيف مونيتور:

```
Type     : HTTP(s)
URL      : http://<الآي بي>:<البورت>/health
Interval : 5 minutes
```

كده هتعرف فوراً لو البوت وقع، والموقع بيفضل صاحي.

---

## 7️⃣ عايز دومين حقيقي؟

اللينك الافتراضي فيه بورت (`:25591`) وشكله مش احترافي. الحلول المجانية:

1. **دومين مجاني** من <https://www.freenom.com> أو <https://nic.eu.org>
2. حسابه في **Cloudflare** (مجاني) → أضف الدومين
3. في **DNS** اعمل ريكورد:
   - `A` أو `CNAME` → يشاور على `node-xx.wispbyte.com`
4. الـ Proxy (السحابة البرتقالي) لازم يكون **مقفول** لأن البورت مش 80/443

> 💡 أسهل بديل: خد لينك مختصر مجاني من <https://dub.co> أو <https://short.io>
> ويشاور على `http://node-xx.wispbyte.com:25591`.

---

## 8️⃣ استكشاف الأخطاء

| المشكلة | السبب والحل |
|---|---|
| الصفحة مش بتفتح | `API_SERVER_ENABLED` لسه `false` → خليه `true` واعمل Restart |
| `Could not start API server` | البورت مشغول — سيب `API_PORT` فاضي عشان ياخد `SERVER_PORT` |
| الأرقام كلها `—` | `/stats` مش بيرد — بص على اللوج |
| «البوت مش متصل» في الصفحة | البوت لسه بيقوم، أو `/stats` راجع خطأ |
| زرار السيرفر رمادي | ما حطّيتش `SUPPORT_SERVER_URL` |
| الصفحة إنجليزي وأنا عايزها عربي | امسح الـ localStorage من المتصفح، أو `WEBSITE_DEFAULT_LANG=ar` |

---

## 9️⃣ ملاحظات تقنية

- **صفحة واحدة، طلب واحد** — الـ CSS والجافاسكريبت جوه الـ HTML. مفيش CDN
  ولا خطوط خارجية، فمفيش حاجة برّانية ممكن توقّع الموقع.
- **الحجم ~40 كيلوبايت** وبتتخزّن في الذاكرة مرة واحدة عند أول زيارة.
- **ETag** مفعّل → الزيارة التانية بترجع `304` من غير تحميل.
- **اللغتين موجودين في الصفحة** والتبديل بيحصل بـ CSS — فوري ومن غير إعادة تحميل،
  واختيارك بيتحفظ في المتصفح.
- **الاستهلاك:** أقل من 1MB رام إضافية، و0% معالج وقت السكون.
- **41 اختبار** بيغطّوا الصفحة، منهم 9 بيشغّلوا الجافاسكريبت في متصفح حقيقي (jsdom).
