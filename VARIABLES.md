# 🔧 المتغيّرات — تفعيل كل خدمات البوت

> نسخ ولصق في **Startup → Variables** في لوحة Wispbyte.

---

## 🔗 ربط الدومين بحساب ديسكورد — خطوتين

عشان `ccimmune.wisp.uno` يظهر في بروفايلك على ديسكورد.

في نافذة **Connect your Domain** اللي فاتحة عندك، فيه خانتين:

| الخانة | فيها إيه | تعمل بيها إيه |
|---|---|---|
| **Name** | `https://ccimmune.wisp.uno/.well-known/dis...` | ❌ **متنسخهاش** — دي بس بتقولك الملف هيتحط فين |
| **Content** | `dh=e2cc6b6c8b9d4cd488b51bf405f2b5bab92d` | ✅ **دي اللي محتاجها** |

### 1️⃣ في Wispbyte → Startup → Variables

اضغط **Copy** جنب **Content**، وضيف المتغيّر ده:

```
DISCORD_DOMAIN_VERIFICATION = dh=e2cc6b6c8b9d4cd488b51bf405f2b5bab92d
```

> 💡 لو نسخت الكود من غير `dh=` مفيش مشكلة — البوت بيضيفها لوحده.
> ولو لصقت الـ **Name** بالغلط، البوت بيتجاهلها بدل ما ينشر ملف غلط.

### 2️⃣ اعمل Restart، وبعدين اضغط **Verify**

عشان تتأكد إن الملف نزل صح قبل ما تضغط Verify، افتح اللينك ده في المتصفح:
```
https://ccimmune.wisp.uno/.well-known/discord
```
المفروض تشوف **السطر ده بس** ومفيش أي حاجة تانية:
```
dh=e2cc6b6c8b9d4cd488b51bf405f2b5bab92d
```

لو شفته ✅ ارجع لديسكورد واضغط **Verify** — هيتوثّق على طول.
لو ظهرلك `{"error":"not found"}` يبقى المتغيّر مش متحطوط أو البوت ما عملش Restart.

> ⚠️ **متستخدمش زرار «Verify using DNS»** — ده محتاج تحكم في DNS بتاع
> `wisp.uno` وإنت مش مالكه. طريقة **HTTPS** (الملف) هي الصح في حالتك.

---

## ✅ البوت شغال — والداشبورد جاهزة

موقعك: **https://ccimmune.wisp.uno** · الحالة: `healthy` · الداتابيز: `ok`

### اللي كان ناقص واتصلح دلوقتي

| المشكلة | الحالة |
|---|---|
| مفيش لينك للداشبورد في الموقع | ✅ اتضاف في النافبار + زرار في الواجهة |
| البوت مش عارف عنوانه (`ccimmune.wisp.uno`) | ✅ بيتعرّف **تلقائياً** من أول زيارة — مش محتاج تكتب حاجة |
| أمر `/dashboard` مش ظاهر | ✅ موجود، وهيتنشر لوحده لأن التوكن بقى مظبوط |

---

## 🖥️ إزاي تدخل الداشبورد

### الطريقة 1 — الرابط السرّي (شغالة دلوقتي، مفيش إعداد)

اكتب في أي روم في سيرفرك:
```
/dashboard
```
البوت يبعتلك **في الخاص** رابط زي ده:
```
https://ccimmune.wisp.uno/dashboard/login?token=xxxxxxxx
```
صالح **10 دقايق** و**مرة واحدة بس**.

> 🔒 ما تديش الرابط ده لحد. أي حد معاه يقدر يتحكم في سيرفراتك.
>
> الأمر مصرّح لـ **Manage Server** بس. أسماؤه العربية: `لوحة` · `داشبورد`

### الطريقة 2 — زرار «سجّل الدخول بديسكورد» (اختياري)

**إنت عندك دومين `https`، يعني ده ممكن يشتغل عندك!** محتاج خطوتين:

**1.** روح [Discord Developer Portal](https://discord.com/developers/applications) →
اختار التطبيق → **OAuth2** → انسخ **Client Secret**
(لو مش ظاهر اضغط **Reset Secret**)

**2.** في نفس الصفحة تحت **Redirects** اضغط **Add Redirect** والصق:
```
https://ccimmune.wisp.uno/dashboard/callback
```
اضغط **Save Changes**

**3.** في Wispbyte → **Startup → Variables** ضيف:
```
CLIENT_SECRET        = <الـ Client Secret اللي نسخته>
DASHBOARD_PUBLIC_URL = https://ccimmune.wisp.uno
```

اعمل **Restart** — الزرار هيظهر لوحده في `/dashboard`.

> الزرار مخفي دلوقتي **عن قصد**: من غير `CLIENT_SECRET` ديسكورد هيرفض الدخول،
> فبدل ما نوريك زرار بيقع، بنخفيه لحد ما يبقى جاهز.

---

## 🖥️ الداشبورد — لوحة تحكم على الويب ✅

**أيوة، في نفس الموقع بتاع البوت.** مش استضافة تانية ولا تكلفة زيادة:

```
http://<الآي بي>:<البورت>/            ← الصفحة التعريفية (زي ما هي)
http://<الآي بي>:<البورت>/dashboard   ← لوحة التحكم (الجديد)
```

### إزاي تدخل؟

اكتب **`/dashboard`** في السيرفر → البوت يبعتلك في الخاص رابط دخول
صالح **10 دقايق** و**مرة واحدة بس**.

> 🔒 ما تديش الرابط ده لحد. أي حد معاه يقدر يتحكم في سيرفراتك.

### إيه اللي فيها؟

| التبويب | بتعمل إيه |
|---|---|
| ⚡ **الإعداد** | نفس الويزارد بتاع `/setup` — بيوريك اللي موجود واللي ناقص، وإنت تختار بالماوس |
| 💬 **القنوات** | 24 وظيفة (ترحيب · قوانين · تكت · بانلز...) تربط كل واحدة بقناة من قائمة منسدلة |
| 🧩 **الخدمات** | تفتح وتقفل أي موديول بسويتش. اللي معطّل لسبب تقني بيظهر «غير متاح» مع السبب |
| ⚙️ **عام** | البريفكس · اللغة · سلاش/بريفكس · كارت الترحيب · إحصائيات السيرفر |

كل تغيير بيتحفظ **فوراً** وبيظهر في ديسكورد على طول — نفس قاعدة البيانات.

### الأمان

| الحماية | التفاصيل |
|---|---|
| **صلاحيات** | لازم تكون **Manage Server**. بتتفحص في **كل طلب**، مش مرة واحدة |
| **الروابط** | مرة واحدة · 10 دقايق · 256-bit عشوائي |
| **الجلسة** | 8 ساعات، وبتموت لو البوت عمل ريستارت |
| **CSRF** | كل تعديل محتاج توكن من الجلسة |
| **الكتابة** | قائمة بيضاء صارمة — المتصفح **مش** قادر يكتب أي حقل غير المسموح |
| **التحقق** | أي ID قناة/رتبة بيتأكد إنه موجود في سيرفرك فعلاً |

اتجرّب فعلياً: طلب من غير دخول ❌ · رابط مستعمل تاني ❌ · بدون CSRF ❌ ·
عضو عادي ❌ · محاولة كتابة `premium.tier` ❌ · قفل موديول أساسي ❌

### 🔵 زرار «سجّل الدخول بديسكورد» — لو عايزه

**ديسكورد بيرفض أي رابط `http`** (غير localhost)، ولينكك الحالي `http` وببورت.
عشان كده الزرار مخفي دلوقتي — عمداً، مش عطل.

عشان يشتغل محتاج **رابط `https` ثابت** يعني **دومين**:

```
DASHBOARD_PUBLIC_URL = https://your-domain.com
CLIENT_SECRET        = <من Developer Portal → OAuth2>
```
وتضيف في **Developer Portal → OAuth2 → Redirects**:
```
https://your-domain.com/dashboard/callback
```

> ⚠️ **Cloudflare Tunnel المجاني (بدون دومين) مش هينفع هنا** — بيدي لينك
> عشوائي **بيتغيّر كل ريستارت**، وديسكورد بيطابق الرابط حرف بحرف، فهتضطر
> تعدّله في Developer Portal بعد كل مرة. عايز OAuth؟ خد دومين (~10$/سنة).

**الرابط السرّي شغال دلوقتي من غير أي من ده** — والكود جاهز للاتنين.

### المتغيّرات

| المتغيّر | الافتراضي | ملاحظات |
|---|---|---|
| `API_SERVER_ENABLED` | `true` | لازم `true` |
| `DASHBOARD_ENABLED` | `true` | يقفل `/dashboard` لو خليته `false` |
| `WEBSITE_URL` | — | **حطه** عشان الروابط تتبني صح: `http://<ip>:<port>` |
| `DASHBOARD_PUBLIC_URL` | — | للـ OAuth بس — لازم `https` |
| `CLIENT_SECRET` | — | للـ OAuth بس |

---

## 🧙 السيتب بقى تفاعلي — بيسألك الأول ✅

**المشكلة اللي قلتها:** `/setup auto` كان بيعمل قنوات جديدة حتى لو إنت عامل
قناة زيها قبل كده، فبقى عندك تكرار.

**الحل — السيتب دلوقتي 3 خطوات:**

```
1) بيفحص السيرفر         →  بيدوّر على كل قناة/كاتيجوري/رتبة موجودة عندك
2) بيعرض عليك الخطة       →  «هيتربط بالموجود عندك: 4» / «هيتعمل جديد: 22»
3) مستني موافقتك          →  مفيش أي حاجة بتتعمل قبل ما تضغط 🚀 تنفيذ
```

### الأوامر

| الأمر | بيعمل إيه |
|------|-----------|
| `/setup` أو `/setup wizard` | **الواجهة التفاعلية** (المفضّل) |
| `/setup auto` | نفس المعاينة — مش بيعمل حاجة على طول |
| `/setup auto confirm:true` | ينفّذ على طول من غير معاينة (الطريقة القديمة) |
| `/setup status` | لوحة صحة الإعدادات |

### الأزرار اللي هتشوفها

| الزر | بيعمل إيه |
|-----|-----------|
| 💬 **اختار القنوات** | قائمة بكل القنوات اللي هتتعمل — **شيل علامة الصح** عن أي حاجة مش عايزها |
| 🎭 **الرتب** | نفس الفكرة لرتب الرانك + زر يقفل إنشاء الرتب خالص |
| 🔗 **اربط قناة موجودة** | تختار الوظيفة (ترحيب/قوانين/تكت...) ثم القناة بتاعتك من قائمة ديسكورد |
| 🚫 **من غير إنشاء** | «مش عايز أي قناة جديدة» — يربط الموجود بس |
| 🚀 **تنفيذ** | ينفّذ الخطة اللي إنت وافقت عليها |
| ❌ **إلغاء** | يقفل من غير أي تغيير |

### إزاي بيعرف إن القناة عندك بالفعل؟

بيشيل كل الزخرفة من الاسم قبل المقارنة، فالأسماء دي كلها بتتعرف صح:

```
💬・general-chat   →  الـ general slot
📜 Server Rules    →  الـ rules slot
┃🎫┃open-a-ticket  →  قناة التكت
قوانين-السيرفر     →  الـ rules slot (بالعربي كمان)
```

ولو ماعرفش يطابق حاجة — بيحطها في قائمة «هيتعمل جديد» وإنت تقرر.

⏱️ الجلسة بتفضل شغالة **15 دقيقة**، وبعدها شغّل `/setup` تاني.

---

## 🎫 التكت مش تحت كاتيجوري — اتصلح ✅

**السبب:** `/setup auto` كان بيعمل 4 كاتيجوريهات (CC IMMUNE · GAMEPLAY ·
COMMUNITY · AUDIO) لكن **عمره ما عمل واحدة للتكت**. كان فيه خيار يدوي بس
(`/setup channel feature:ticketCategory`)، وإنت طبعاً ما استخدمتوش.

ولإن `ticketCategory` فاضي، كل تكت كان بيتفتح **روم سايب في آخر السيرفر**.

**الحل:** `/setup auto` بقى يعمل:

```
『 SUPPORT 』                    ← كاتيجوري مخفية عن الأعضاء
   └── #open-a-ticket           ← روم للزرار (للقراءة بس)
   └── ticket-0001 ...          ← التكتات بتتفتح هنا
```

الكاتيجوري **مخفية عن `@everyone`** عشان محدش يشوف تكتات غيره — وكل تكت
بيدّي صلاحية لصاحبه والمشرفين بس.

اختبرته:
```
categories: CC IMMUNE | GAMEPLAY | COMMUNITY | AUDIO | SUPPORT ✔
ticketCategory saved: ✔
```

بعد ما ترفع، شغّل `/setup auto` تاني — هيعمل الناقص بس من غير ما يكرّر
الموجود.

---

## 🎨 رسالة الترحيب — بقت بكارت احترافي

عملت كارت ترحيب بتصميم كامل زي اللي بعته:

```
╔══════════════════════════════════════════════╗
║   ⭕ صورة       WELCOME TO                    ║
║   العضو        CC IMMUNE                     ║
║              [ GLAD TO HAVE YOU HERE! ]      ║
║              Gbril                           ║
║              Member #26                      ║
╚══════════════════════════════════════════════╝
```

### ليه مش صورة عادية؟

الحل المعتاد مكتبة `canvas` — بس دي **61 ميجا** من ملفات تشغيل، وإحنا
شغالين على 384 ميجا رام. مش منطقي ندفع التمن ده لصورة واحدة.

فعملتها **SVG**: نص بنولّده في أجزاء من الثانية، ديسكورد بيعرضه عادي،
و**الحجم 2.5 كيلوبايت بس** — يعني صفر تأثير على الرام.

### حاجات اتظبطت
- **الأسماء بتتفلتر** — اسم فيه أكواد مش هيكسر الكارت (اختبرته بمحاولة حقن)
- **الأسماء الطويلة بتتقص** بدل ما تخرج برّه التصميم
- لو صورة العضو مش متاحة، بيرسم **أول حرف من اسمه** في دايرة بدل مكان فاضي
- **بالعربي والإنجليزي** حسب لغة السيرفر
- لو حصل أي خطأ، الرسالة بتتبعت عادي من غير كارت — الترحيب ما بيقفش

### لو مش عايز الكارت
```
welcomeCard = false
```
هيرجع للشكل القديم بصورة صغيرة في الركن.

---

## 🎛️ لوحة التحكم الصوتية — جديد

```
/music panel
```

رسالة واحدة بتفضل محدّثة مع كل ضغطة:

```
🎛️ لوحة التحكم الصوتية
🎵 Song Title
-# Artist
⏱️ 3:00
━━━━━━━━━━━━━━━
▶️ شغّال · 🔊 80% · 🔂 تكرار: الأغنية · 📜 في الطابور: 2

[⏸️ إيقاف] [⏭️ التالي] [⏹️ إيقاف تام] [🔂] [🔀]
[🔉] [🔊] [📿 القرآن] [🔄]
```

**التحكم الكامل:** إيقاف/تشغيل · التالي · إيقاف تام · تكرار (مقفول/أغنية/قائمة)
· خلط · رفع وخفض الصوت · تبديل للقرآن.

**حاجات مهمة اتظبطت:**
- الأزرار اللي مالهاش معنى **بتتقفل** — مش هتلاقي «التالي» شغّال والبوت ساكت
- «التالي» بيتقفل وقت القرآن (بث مستمر، مفيش «تالي»)
- **لازم تكون في نفس الروم الصوتي** عشان تتحكّم — عشان محدش يوقف الأغاني على الناس

---

## 🐛 `/setup panels` مكانش ظاهر — غلطة مني

التعديل اللي عملته المرة اللي فاتت **ما اتطبّقش أصلاً**. أنا كتبت الكود
وافترضت إنه اتحفظ من غير ما أتأكد — والدالة اتضافت بس **تعريف الأمر نفسه
لأ**، فما ظهرش في القايمة.

دلوقتي اتأكدت بالفحص:
```
subcommands: status, auto, channel, panels, export
has panels : ✔
```

### وباجين في قراءة الأوامر لقيتهم وأنا بتأكد

**1. `channel:` مكانش شغّال**
ديسكورد بيعرض الخيارات بالشكل ده (`channel:`)، فالناس بتكتبه كده في البريفكس.
البوت كان بيفهم `channel=` بس:
```
قبل:  ccsetup panels channel:#روم   → ✘ خطأ
بعد:  ccsetup panels channel:#روم   → ✔
```
والروابط والأوقات (`12:30`) لسه سليمة — بيتقسم بس لو الكلمة قبل النقطتين
اسم خيار حقيقي.

**2. `!play` بكلمتين أو أكتر كان بيفشل**
```
قبل:  !play never gonna give you up  → ✘ VAL-001 on source
بعد:  !play never gonna give you up  → ✔
```
السبب: الكود كان بيدّي النص الكامل **لآخر خيار بس**، و`/play` معرّف
`query` الأول وبعده `source`. فـ «gonna give you up» كانت بتروح لـ `source`
اللي بيقبل 3 قيم بس.

دلوقتي `!play never gonna give you up scsearch` بيفهم الاتنين صح.

---

## 🎵 البلاي ليست بتقف بعد أغنية — اتصلح ✅

**السبب:** البوت كان بيربط أحداث النودات بس، ومكانش بيربط **أحداث المشغّل
نفسه**. يعني لما الأغنية تخلص، **مفيش حاجة كانت بتشغّل اللي بعدها**.

فحصت الكود ولقيت مفيش أي معالج لـ `end` خالص:
```
this.shoukaku.on('ready' | 'error' | 'close' | 'disconnect')   ← النودات بس
player.on('end')  ← مش موجود أصلاً
```

**الحل:** ربطت دورة حياة الأغنية كاملة:

| الحدث | السلوك |
|---|---|
| `end` (خلصت) | يشغّل اللي بعدها |
| `exception` (فشلت) | يتخطّاها ويكمّل |
| `stuck` (علّقت) | يتخطّاها ويكمّل |
| `end` (replaced) | **يتجاهله** — ده سكيب يدوي، لو اتعامل معاه هيتخطّى اتنين |
| `closed` | ينضّف الحالة عشان `/play` الجاي يشتغل |

واللوب (`/music loop`) بيشتغل صح لأن المنطق بتاعه في مكان واحد.

اختبرته:
```
after play : A        | queue left: 2
after ends : A,B,C    | queue left: 0  ✔ البلاي ليست كاملة
after error: D,E                       ✔ تخطّى الفاشلة
on replaced: ignored                   ✔ مفيش تخطّي مزدوج
```

---

## 🎫 التكت — «Something went wrong» ✅

**السبب:** الكود كان بيحاول يعمل الروم على طول من غير ما يتأكد من حاجتين:
1. البوت عنده صلاحية **Manage Channels**؟
2. الكاتيجوري المحفوظة لسه موجودة؟

لو الكاتيجوري اتمسحت أو مليانة (ديسكورد بيسمح بـ 50 روم بس فيها)، ديسكورد
بيرفض الطلب كله والبوت كان بيرد برسالة عامة مالهاش أي معنى.

**الحل:**
- بيتأكد من الصلاحية **قبل** المحاولة، ويقولك بالظبط اللي ناقص
- لو الكاتيجوري مش صالحة، **بيعمل الروم برّاها** بدل ما يفشل
- كود خطأ جديد `MOD-011` بيشرح الأسباب الثلاثة والحل لكل واحد

---

## 📊 البانلز مش لاقيها؟

البانلز **شغالة** — اختبرتها بقاعدة بيانات حقيقية والخمسة اتنشروا. المشكلة
إنها بتتنشر مع `/setup auto` بس، ولو إنت بنيت السيرفر بإيدك أو الأمر ما
كملش، مش هتلاقيها.

**ضفت أمر مباشر:**
```
/setup panels channel:#اسم-الروم
```

بيعمل كل حاجة في خطوة واحدة: يحفظ الروم، ينشر الخمس بانلز، ويشغّل التحديث
كل 6 ساعات. ولو حصلت مشكلة بيقولك السبب بالظبط (صلاحيات ناقصة مثلاً) بدل ما
يسكت.

ولو عايز تعيد نشرهم بعدين:
```
/setup panels
```

---

## 🔴 «Module is disabled» على كل بطل — اتصلح ✅

### ده باج من إصلاح النهاردة، وأنا السبب

لما قفلت الوسيط المجتمعي (`MLBB_API_BASE`) عشان أشيل تحذير `mlbbLive`،
مكنتش واخد بالي إن فيه موديول **مشروط بالمتغيّر ده**:

```js
key: 'mlbb-api',
requiredEnv: ['MLBB_API_BASE'],   ← فضّيته، فالموديول قفل نفسه
```

ولإن `mlbb-heroes` بيعتمد على `mlbb-api`، **اتقفل معاه بالتتالي** — وكل
أوامر الأبطال بقت ترد بـ «Module is disabled».

**غلطة مني:** صلّحت التحذير من غير ما أتأكد مين تاني بيعتمد على المتغيّر ده.

### السبب الجذري

المتغيّر ده بيظبّط **الوسيط الاختياري** — مش المصدر الأساسي. المصدر الأساسي
هو `MOONTON_API_BASE` وليه قيمة افتراضية دايماً، وورا كده ملف محلي بـ 131
بطل. يعني الموديول **مكانش المفروض يعتمد على المتغيّر ده أصلاً**.

**الحل:** شلت الشرط. الموديول دلوقتي شغال دايماً، والخدمة نفسها هي اللي
بتقرر تقفل نفسها لو كل المصادر وقعت فعلاً.

### فحصت كل الموديولات

عملت فحص شامل لأي موديول مشروط بمتغيّر مش متظبّط:
```
✔ database   needs MONGO_URI        ← متظبّط
✘ redis      needs REDIS_URL        ← مقفول عن قصد (اختياري فعلاً)
✔ quran      needs QURAN_STREAMS    ← متظبّط
✔ prayer     needs PRAYER_API_BASE  ← متظبّط
```
`redis` بس هو المقفول، وده صح.

### النتيجة

```
قبل:  40/42 · كل أوامر الأبطال مقفولة
بعد:  41/42 · كل حاجة شغالة

✔ mlbb-api      ✔ mlbb-heroes    ✔ mlbb-tierlist
✔ info-panels   ✔ ai-coach
```

واختبرت الأزرار كمان: `overview ✔ · skills ✔ · build ✔ · lore ✔`

**وضفت اختبار** بيرفض أي موديول أساسي يبقى مشروط بمتغيّر اختياري — عشان
الغلطة دي ما تتكررش.

---

## 🟡 `mlbbLive — unreachable` — الحل النهائي ✅

### إيه هي أصلاً؟

`mlbbLive` مش خدمة أساسية — دي كانت **وسيط مجتمعي** كنا بنستخدمه قبل ما
أوصل للـ API الرسمي بتاع Moonton. بعد ما ربطنا Moonton مباشرة، بقت زيادة.

### فحصت المشكلة بجد

```
mlbb.rone.dev         → مفيش DNS خالص (اتأكدت من Google DNS مش من الساندبوكس بس)
rone.dev              → شغال (يعني الدومين الأصلي حي، بس الساب دومين اتشال)
openmlbb.fastapicloud → 404 على كل حاجة حتى الصفحة الرئيسية
```

يعني **الاتنين اتشالوا فعلاً**، والتوثيق بتاع المشروع الأصلي قديم.

### الحل

بصّيت على اللي الوسيط ده بيقدّمه، ولقيت إن **Moonton المباشر بيغطّي كل
حاجة**:

| البيانات | المصدر دلوقتي |
|---|---|
| 133 بطل | ✅ Moonton مباشرة |
| المهارات والصور | ✅ Moonton مباشرة |
| نسب الفوز والحظر | ✅ Moonton مباشرة |
| الكاونترات | ✅ Moonton مباشرة |
| الأيتمات + الأيقونات | ✅ محفوظة جوه البوت |

**فخلّيت الوسيط اختياري ومقفول افتراضياً.** مش بيتشال من الكود — لو ظهر
بديل شغال في المستقبل، تحطه في متغيّر واحد وهيشتغل.

### النتيجة

```
قبل: 🟡 mlbbLive — unreachable: ENOTFOUND mlbb.rone.dev
بعد: ⚪ mlbbLive — optional wrapper not configured (Moonton direct is primary)
```

**التحذير اختفى** لأنه بقى وصف دقيق للواقع: خدمة اختيارية مقفولة عن قصد،
مش خدمة مكسورة.

### وباج تاني لقيته وأنا بصلّح

الـ `ping()` اللي البوت بيستخدمه عشان يتأكد إن الـ API رجع، كان بيفحص
**المسار القديم على السيرفر الميت**:
```js
await this.http.json(`${this.base}/hero-list/`)   // ← سيرفر مش موجود
```
يعني الموديول **عمره ما كان هيرجع تلقائياً** حتى لو Moonton شغال 100%.
دلوقتي بيفحص المصدر الأساسي فعلاً:
```
ping: true ✔
```

---

## 🇸🇦 الترجمة العربية — بقت زي اللعبة

### الأول: اختبرت العربي الرسمي من الـ API

جرّبت كل الطرق الممكنة:
```
x-lang: ar          → Duality  (إنجليزي)
X-Lang: ar          → Duality  (إنجليزي)
Accept-Language: ar → Duality  (إنجليزي)
X-Lang + X-AppId    → Duality  (إنجليزي)
```
وحتى صفحة الموقع العربي `m.mobilelegends.com/ar/hero` **نفس محتوى الإنجليزي
بالظبط** (نفس الـ md5).

**النتيجة:** Moonton **مش بتوفّر أسماء المهارات بالعربي** في الـ API العام.
مكنتش هقولك ده غير بعد ما أتأكد بنفسي.

### الحل: قاموس بمصطلحات اللاعبين الحقيقية

المشكلة في الترجمة القديمة إنها كانت **حرفية**:
```
Tank → دبابة    ✘  محدش بيقول كده
Mage → ساحر     ✘
```

بنيت قاموس بـ **160 مصطلح** باللي اللاعبين العرب بيقولوه فعلاً:

| إنجليزي | القديم ✘ | الجديد ✔ |
|---|---|---|
| Tank | دبابة | **تانك** |
| Mage | ساحر | **ماج** |
| Marksman | رامي | **ماركسمان** |
| Support | داعم | **سابورت** |
| Mid Lane | المسار الأوسط | **الميد** |
| Jungle | الغابة | **الجانغل** |
| Burst | انفجار | **بيرست** |

**النتيجة في الإمبد:**
```
🎭 الدور   │ 🔮 ماج
🛣️ المسار  │ 🎯 الميد
🏷️ التصنيف │ `بيرست` `تحكّم`
```

**قاعدة مهمة:** أسماء الأبطال والمهارات والأيتمات **ما بتتترجمش أبداً** —
اللاعبين بيدوّروا ويتكلموا بالأسماء الإنجليزية. ده مقفول باختبار.

---

## 🎒 صور الأيتمات في البيلد

**السبب:** إمبد ديسكورد بيقبل **صورة واحدة بس**. فمستحيل يعرض 6 أيقونات
مرة واحدة — علشان كده كنت شايف الأول بس.

**الحل:** كل أيتم بقى **ليه زرار**:
```
[1. Magic Shoes] [2. Holy Crystal] [3. Lightning Truncheon] [4. Genius Wand] [5...]
```
اضغط أي واحد → يفتح الأيتم **بصورته الحقيقية** وإحصائياته وسعره وتأثيره،
والزرار بتاعه بيتقفل عشان تعرف إنت فين.

ولو أيتم مالوش صورة عندنا، البوت **بيدوّر عليها لحظياً** قبل ما يعرض.

اختبرته:
```
view:build → 🎒 Luo Yi — البيلد   + صف أزرار بالأيتمات
item:0     → 🎒 Magic Shoes        thumb ✔
item:2     → 🎒 Lightning Truncheon thumb ✔
```

---

## 🔘 الأزرار — الباج الحقيقي اتحدد أخيراً ✅

الإصلاح اللي فات كان **جزء من المشكلة بس**. كان فيه باج تاني أعمق.

### السبب

الزرار كان بيقرا لغة المستخدم كده:
```js
const locale = client.services.locale?.resolve?.(interaction) ?? 'en';
```

فيه **غلطتين** في السطر ده:
1. `resolve()` دالة **async** — بترجّع Promise مش نص
2. بتتوقّع `{userId, discordLocale}` — مش الـ interaction نفسه

أثبتّها بالتشغيل:
```
القديم →  typeof: object  | is Promise: true  | locale === 'ar' ?  false
الجديد →  value: "ar"                          | locale === 'ar' ?  true
```

يعني `locale` كان **وعد معلّق (Promise)** مش نص. فكل مقارنة `locale === 'ar'`
بترجع false، وباني الإمبد بياخد **كائن** بدل نص — وبيرمي استثناء.

الاستثناء ده هو اللي كان بيطلع **«Something went wrong fetching that»**.

**الحل:** `await` + تمرير الشكل الصح، مع احتياطي لو الخدمة وقعت. وضفت
**اختبار آلي** بيفحص كل المكوّنات ويرفض أي `resolve()` من غير `await`.

اختبرت كل التبويبات بالخدمة الحقيقية:
```
overview ✔   skills ✔   build ✔   lore ✔   counters ✔
```

---

## 🖼️ الأيقونات — بقت تيجي من الـ API

### باج تاني: المزامنة كانت بتمسح الأيقونات

```js
this.liveItems = items;   // ← بيستبدل القايمة كلها
```

الـ API الحي بيرجّع أيتمات أكتر وأحدث، **بس من غير أيقونات**. فأول ما
المزامنة تنجح، كل الأيقونات كانت بتضيع.

**الحل:** بقى **يدمج** بدل ما يستبدل — الأسماء والإحصائيات من الـ API،
والأيقونات من اللي عندنا.

### وكمان: بحث تلقائي عن الأيقونات الناقصة

لو نزل أيتم جديد في اللعبة مش عندنا صورته، البوت **بيدوّر عليها لحظياً**
ويحفظها:
```
Starlium Scythe        ✔ Starlium_Scythe.png     ← أيتم جديد، لقى صورته
Blade of Despair       ✔ Blade_of_Despair.png
Definitely Not An Item ✘ none                    ← مش موجود، وبيفتكر كده
```
بيحفظ النتيجة السلبية كمان عشان ما يدوّرش على نفس الحاجة كل مرة.

---

## 🔘 الأزرار مش شغالة — الباج الأساسي (الجزء الأول) ✅

**السبب:** الأزرار كانت بتنادي الخدمة باسم غلط:
```js
client.services.mlbb      ← الأزرار بتدوّر على ده
container.register('mlbbApi', ...)  ← الاسم الحقيقي
```
فبترجع `undefined` وتطبع «Hero data service is unavailable» — علشان كده
**ولا زرار كان بيشتغل**.

صلّحته، وعملت **فحص آلي لكل المكوّنات** يتأكد إن كل خدمة بينادوها مسجّلة
فعلاً، عشان الغلطة دي ما تتكررش.

اختبرت كل الأزرار:
```
overview ✔   skills ✔   counters ✔   build ✔   lore ✔
skill:0  ✔   skill:3 ✔
```

---

## ✨ المهارات — بقت تفاعلية بالكامل

كل بطل دلوقتي فيه **صف أزرار للمهارات**:
```
[سلبية · Duality] [م1 · Dispersion] [م2 · Rotation] [الأخيرة · Diversion]
```

اضغط أي مهارة → تشوف:
- **أيقونة المهارة نفسها** كبيرة
- الوصف الكامل (من غير اختصار)
- 🏷️ التصنيف: `Burst` `CC` `AOE` `Slow` `Teleport`
- ⏱️ التبريد والمانا: `CD: 12  Mana Cost: 75`

والسلبية (Passive) ليها زرار مستقل زي أي مهارة.

---

## 🎒 الأيتمات — الصور رجعت

**السبب:** الأيتمات كانت بتيجي من الـ API المجتمعي اللي واقع، والملف المحلي
**مفهوش صور خالص**.

**الحل:** جبت الأيقونات الحقيقية من ويكي اللعبة (مرخّصة CC-BY-SA) وحفظتها
**جوه المشروع** — يعني شغالة حتى لو كل الـ APIs وقعت:
```
الأيقونات: 44/45 ✔   (اتأكدت إنها بتحمّل فعلاً — HTTP 200)
```

### وباجين تانيين لقيتهم وأنا بختبر:

**1. `/item info` كان بيقع** — الأيتمات الأوفلاين بتخزّن الإحصائيات كـ **نص**
والـ API كـ **قائمة**. الكود كان بيفترض قائمة دايماً:
```
TypeError: item.stats.map is not a function
```

**2. البيلد كان بيرجع فاضي** — الأوفلاين بيستخدم `category: "attack"`
والـ API بيستخدم `type: "Attack"`. الكود كان بيقرا واحد بس:
```
قبل: Marksman → 0 أيتم
بعد: Marksman → 6 أيتمات بصورهم ✔
```

---

## 📖 القصص والمناطق — جديد

### تبويب «القصة» لكل بطل
فيه قصة البطل الكاملة + **السبلاش آرت** + المنطقة اللي هو منها + الأبطال
التانيين من نفس المنطقة + رابط الصفحة الرسمية.

### 10 مناطق من عالم اللعبة

| المنطقة | الوصف |
|---|---|
| 🏰 إمبراطورية مونيان | أكبر إمبراطورية بشرية، فرسانها بيحرسوا الجبهة ضد الهاوية |
| 🕳️ الهاوية | طاقة فاسدة بتدّي قوة رهيبة بتمن الإنسانية |
| 🌊 أراضي كاديا النهرية | مدن حرة ومرتزقة رحّالة |
| ⚙️ إروديشيو | مدينة العلم والاختراع |
| 🎰 لوس بيكادوس | مدينة صحراوية بلا قانون |
| 🌲 غابات أزريا | الإلف والأرواح بيحرسوا الطبيعة |
| ❄️ الوادي الشمالي | قمم متجمّدة ومحاربون من دم الشمال |
| ✨ القدماء | تنانين وآلهة منسيّة |
| 🏜️ الصحراء الغربية | مدن مدفونة ولعنات قديمة |
| 🌑 عالم الظلال | بين الحياة والموت |

**92 بطل** متوزّعين على المناطق. أمر جديد: `/hero lore <البطل>`

---

## 🧠 الكوتش بقى يشرح فعلاً

**السبب:** الكوتش كان **عمره ما شاف الأيتمات** — فلما تسأله عن بيلد، بيرد
كلام عام لأنه مش عارف أي أيتم موجود في اللعبة أصلاً.

**الحل:** بقى يستلم قائمة الأيتمات المناسبة للبطل بأسمائها وإحصائياتها
وباسيفاتها:
```
<item_options>
Blade of Despair: +160 Phys ATK, +5% Move Speed | passive: +25% damage below 50% HP
Endless Battle: +65 Phys ATK, +250 HP, +10% CDR | passive: +85% true damage after skill
...
```

وضفت قواعد صارمة:
- ممنوع يخترع اسم أيتم أو مهارة
- لو اتسأل عن بيلد **لازم** يدّي 6 أيتمات مرتّبة + سبب لكل واحد
- ممنوع الرد بجملة واحدة
- ورفعت حد الرد من 900 لـ 1400 توكن (كان بيتقطع قبل ما يكمّل)

---

## 📿 القرآن — لقيت السبب الحقيقي أخيراً ✅

الرسالة الجديدة («تعذّر تشغيل البث») معناها إن الإصلاح اللي فات اشتغل: البوت
بقى **يكتشف الفشل ويخرج** بدل ما يفضل ساكت. بس السبب الأصلي كان لسه موجود.

### الاختبار اللي كشف كل حاجة

جرّبت **كل نود × كل مرآة** (4×4):

```
=== jirayu ===
   track   ✔  n0a.radiojar.com
   track   ✔  stream.radiojar.com
   track   ✔  live.mp3quran.net:9702
   track   ✔  qurango.net

=== serenetia / serenetia-v4 / ajiedev-ssl ===
   ✘ فشل    n0a.radiojar.com
   ✘ فشل    stream.radiojar.com
   ✘ فشل    live.mp3quran.net:9702
   ✘ فشل    qurango.net
```

### السبب

النودات دي **مقفّلة خاصية تشغيل الروابط المباشرة** (HTTP source manager).
أثبتّها:

```
serenetia + بحث يوتيوب        → 200 ✔ شغال
serenetia + رابط بث مباشر     → 502 Bad Gateway ✘
```

**علشان كده الأغاني بتشتغل والقرآن لأ** — الأغاني بتيجي من يوتيوب،
والقرآن رابط مباشر.

والبوت كان بيختار النود **الأسرع** — ولو طلع واحد من دول، القرآن يفشل.

### الحل (3 طبقات)

1. **الاكتشاف بقى يفحص دعم البث المباشر** لكل نود، والنود اللي بيدعمه
   بياخد الأولوية حتى لو أبطأ شوية:
   ```
   Auto-discovered Lavalink: jirayu (919ms, search, 20 result(s), http✓)
   ```
2. **الروابط المباشرة بتتوجّه للنود القادر** بس، حتى لو نود تاني أسرع
3. **لو فشل، بيعيد المحاولة على نود تاني** تلقائياً قبل ما يستسلم

اختبرت الحالات الثلاثة:
```
بث مباشر  → ENC-jirayu      ✔ راح للنود الصح
بحث عادي  → ENC-serenetia   ✔ عادي، مفيش توجيه زيادة
لو غلط    → أعاد المحاولة ولقى jirayu ✔
```

---

## 💬 لينك سيرفر الديسكورد — اتضاف ✅

اختبرت اللينك الأول عشان أتأكد إنه سليم:
```
الحالة    : 200 ✔
السيرفر   : CC Immune
الآي دي   : 1529509091146338497   ← مطابق لسيرفرك
الأعضاء   : 26
الصلاحية  : دائم (مش بينتهي)
```

**اتحط كقيمة افتراضية في البوت** — يعني شغّال من غير ما تضيف أي حاجة في
لوحة Wispbyte.

### بيظهر في 3 أماكن تلقائياً:

| المكان | إزاي |
|---|---|
| 🌐 **الموقع** | زرار «ادخل سيرفر الدعم» + لينك في الفوتر (بالعربي والإنجليزي) |
| 💬 `/about` | زرار Support |
| 💬 `/help` | زرار Support |

### لو عايز تغيّره بعدين
في **Startup → Variables**:
```
SUPPORT_SERVER_URL = <اللينك الجديد>
```
المتغيّر ده بيغلب القيمة الافتراضية دايماً — اختبرت ده.

---

## 🌐 الموقع — الإنجليزي واللوجو اتصلحوا

### الباج: الإنجليزي مش شغال

الصفحة فيها اللغتين، والزرار بيغيّر كلاس على `<body>`. **بس الـ CSS اللي
بيخفي اللغة التانية كان ناقص خالص.** فحصت الصفحة المولّدة:

```
عدد بلوكات الإنجليزي في الصفحة:  8
عدد قواعد CSS بتخفي/تظهر اللغة:  0   ← الباج
```

يعني الزرار كان بيشتغل فعلاً، بس مفيش حاجة بترد عليه.

**الحل:** ضفت القاعدتين الناقصين واختبرتهم في متصفح حقيقي:
```
قبل الضغط:  body=lang-ar | الإنجليزي: none  | العربي: ظاهر
بعد الضغط:  body=lang-en | الإنجليزي: ظاهر  | العربي: none
```

### اللوجو

كان في مكانه أيقونة درع عامة. دلوقتي البوت **بياخد صورته الحقيقية من
ديسكورد تلقائياً** — مش محتاج ترفع صورة في أي مكان.

بتظهر في: الهيدر · أيقونة التبويب (favicon) · معاينة اللينك لما تبعته
في أي مكان (og:image).

لو الصورة مش متاحة لأي سبب، بيرجع للأيقونة القديمة بدل ما يبان مكسور.

---

## 📊 البانلز بقت حيّة

### صور حقيقية
كل بانل فيه دلوقتي **سبلاش آرت** بعرض الرسالة + صورة صغيرة في الركن:

| البانل | الصورة | اللون |
|---|---|---|
| 📊 الميتا | سبلاش بطل رقم 1 | 🟣 بنفسجي |
| 🏆 أعلى فوز | سبلاش المتصدّر | 🟢 أخضر |
| 🚫 أكثر حظراً | سبلاش الأكثر حظراً | 🔴 أحمر |
| ⭐ بطل اليوم | سبلاش البطل | 🟡 ذهبي |

### أزرار تفاعلية

```
📊 الميتا / أعلى فوز / أكثر حظراً
   [🔄 تحديث]  [🔀 عرض آخر]

⭐ بطل اليوم
   [🔄 تحديث]  [📋 تفاصيل البطل]  [⚔️ المواجهات]
```

- **🔄 تحديث** — أي حد يقدر يجيب أرقام جديدة من غير ما يستنى 6 ساعات
  (فيه كولداون 30 ثانية لكل عضو عشان ما يتسبّبش في ضغط)
- **🔀 عرض آخر** — نفس الرسالة بتتبدّل بين الميتا وأعلى فوز وأكثر حظراً
- **📋 / ⚔️** — بيفتحوا تفاصيل البطل ومواجهاته على طول

### الصور بتتخزّن
البوت بيحفظ روابط الصور في الذاكرة، فالتحديث مش بيعيد تحميلها كل مرة.

---

## 📿 مشكلة القرآن — اتصلحت

**العرض:** البوت بيدخل الروم الصوتي بس مفيش صوت، و`/quran reciter` بيدي
`INT-001`.

**السبب:** لقيت باج صامت في `play()`:
```js
const player = this.shoukaku?.players.get(guildId);
if (!player) return null;   // ← بيرجع بهدوء من غير أي رسالة
```
لما البث ما يتحمّلش، البوت كان بيدخل الروم ويقعد ساكت **من غير ما يقولك في
إيه**.

**اختبرت كل المرايا الأربعة على نود Lavalink حقيقي:**
```
✔ playable  https://stream.radiojar.com/8s5u5tpdtwzuv
✔ playable  http://live.mp3quran.net:9702/
✔ playable  https://qurango.net/radio/tarateel
✔ playable  https://n0a.radiojar.com/8s5u5tpdtwzuv
```
كلهم شغالين، و**177 قارئ** كلهم روابطهم سليمة.

**الحل:**
1. `play()` بقى **يرمي خطأ واضح** بدل ما يسكت
2. لو المرآة الأولى فشلت، البوت **يجرّب الباقي تلقائياً**
3. لو كلهم فشلوا، **يخرج من الروم** ويقولك رسالة مفهومة بدل ما يفضل ساكت

---

## 📊 بانلز المعلومات التلقائية — جديد

### 5 بانلز في روم واحدة، كل واحد رسالة لوحده

| البانل | فيه إيه |
|---|---|
| 📊 **الميتا** | قائمة تصنيف S/A/B حسب نسبة الفوز الحقيقية |
| 🏆 **أعلى فوز** | أفضل 10 أبطال بالميداليات |
| 🚫 **أكثر حظراً** | أكتر 10 أبطال بيتحظروا + شريط بصري |
| ⭐ **بطل اليوم** | بطل مختلف كل يوم بصورته وقصته وإحصائياته |
| 📈 **إحصائيات السيرفر** | أعضاء · رومات · رتب · بوستات · أوامر · تشغيل |

### نموذج حقيقي من الاختبار:
```
🔥 Tier S
Marcel 58.41% · Masha 57.18% · Rafaela 56.56% · Gloo 56.37% ... +11 more

🚫 أكثر الأبطال حظراً
 1  Paquito ███████ 71.67%
 2  Gloo    ███████ 68.51%
```

### إزاي تشغّلها؟

**تلقائي مع `/setup auto`** — بيعمل روم `tier-list` وينشر البانلز فيها على
طول.

لو الروم موجودة عندك بالفعل:
```
/setup channel feature:tierList channel:#اسم-الروم
```

### بتتحدّث **كل 6 ساعات** لوحدها

البوت **بيعدّل نفس الرسالة** مش بيبعت رسالة جديدة — فالروم بتفضل نضيفة
والرسايل محافظة على مكانها (تقدر تثبّتها pin).

لو حد مسح رسالة بانل بالغلط، البوت بيعيد نشرها **مرة واحدة بس** في التحديث
اللي بعده.

---

## 🔧 4 باجات من اللوج بتاعك — كلها اتصلحت

اللوج اللي بعته كان مفيد جداً. لقيت منه **4 باجات حقيقية**:

### 1️⃣ الموسيقى مقفولة رغم إن النودات اتصلت ✅

اللوج بيقول:
```
⊘ music — Missing required configuration: LAVALINK_HOST
...
[SUCCESS] Lavalink node "jirayu" connected      ← اتصل فعلاً!
```

**السبب:** الموديول كان مشروط بمتغيّر `LAVALINK_HOST` اليدوي، فكان بيتقفل
**قبل** ما الاكتشاف التلقائي يشتغل أصلاً.

**الحل:** شلت الشرط. الاكتشاف التلقائي هو اللي بيحدد، والخدمة بتقفل نفسها
بس لو مفيش أي نود.

**النتيجة: 38/41 → 40/41 موديول.**

### 2️⃣ `/doctor` بيقول `0/3 nodes up` والنودات شغالة ✅

**السبب:** رقم غلط. مكتبة Shoukaku بتستخدم:
```
0 = بيتصل   1 = متصل ✅   2 = بيقطع   3 = مقطوع
```
الكود كان بيعد `state === 2` — يعني **النودات اللي بتقطع** مش المتصلة!

**الحل:** صلّحته لـ `CONNECTED (1)`، وعملته ثابت باسم واضح بدل رقم عايم.

### 3️⃣ سيل أخطاء `429` والنودات بتفصل ✅

اللوج فيه عشرات الأسطر:
```
[WARN] node "ajiedev-ssl" closed (4000) — reconnecting
[ERROR] node "ajiedev-ssl" error reason=Unexpected server response: 429
```

**السبب:** البوت كان بيفتح اتصال بـ **5 نودات مرة واحدة**، والسيرفرات
المجانية بتعتبر ده إساءة استخدام وبتعمل حظر مؤقت (429). وكمان
`ajiedev-ssl` و`ajiedev-plain` **نفس السيرفر** — فكنا بنضربه مرتين.

**الحل:**
- الاتصال بـ **نودين بس** (الباقي احتياطي)
- منع الاتصال المزدوج بنفس السيرفر
- تقليل محاولات إعادة الاتصال وتبطيئها
- تسجيل أول انقطاع بس بدل عشرات الأسطر المكررة

### 4️⃣ الموقع مش بيشتغل — `EADDRNOTAVAIL` ✅

```
[ERROR] [Api] Could not start API server:
        listen EADDRNOTAVAIL: address not available 10.66.66.25:10201
```

**السبب:** Wispbyte بيبعت `SERVER_IP=10.66.66.25`، وأنا كنت بستخدمه. لكن ده
عنوان **خارجي** مش موجود جوه الكونتينر — فالربط بيفشل.

**الحل:** البوت بيربط على `0.0.0.0` (كل الواجهات)، ولو فشل بيجرب تاني
تلقائياً. اختبرته:
```
Cannot bind 10.66.66.25 — retrying on 0.0.0.0
API server listening on 0.0.0.0:24817 · website enabled at /
GET /health -> 200 ✅
```

### 5️⃣ كمان: `ENOTFOUND mlbb.rone.dev`

الـ API المجتمعي كان واقع ساعتها، وخطأه كان **بيلغي المزامنة كلها** حتى لو
Moonton نجح. عزلته — اختبرته والـ API المجتمعي ميت تماماً:
```
Live sync via moonton: 133 heroes ✅
```

---

## ✅ بعد الرفع المفروض تشوف

```
[Bot] Modules: 40/41 enabled            ← بدل 37/41
[MLBB] Live sync via moonton: 133 heroes
[Music] Lavalink node "jirayu" connected
[Api] API server listening on 0.0.0.0:10201 · website enabled at /
```

و `/doctor health`:
```
mlbbApi — Moonton direct · 133 heroes ✅
music   — 2/2 nodes up ✅
```

---

## 🎯 ربط البوت بمصدر Moonton الرسمي مباشرة

طلبت أدوّر على APIs أحسن. فحصت اللي بعتهم كلهم واختبرتهم فعلياً:

| الموقع | النتيجة |
|---|---|
| `mlbb.io` | ❌ مفيش API عام (404 على كل المسارات) |
| `mlbb.gg` | ❌ مفيش API عام |
| `mobile-legends.fandom.com` | ⚠️ فيه API لكن بيرجّع قوالب ويكي مش بيانات منظمة |
| `mobilelegends.com` | ✅ **لقيت الكنز هنا** |

### الاكتشاف

فحصت كود الموقع الرسمي نفسه (`m.mobilelegends.com`) ولقيت إنه بينده على:
```
https://api.gms.moontontech.com/api/gms/source/...
```

**ده الـ API الرسمي بتاع Moonton نفسها.** مفتوح، من غير مفتاح، ومن غير حد
أقصى للطلبات. والمفاجأة إن الـ API المجتمعي اللي كنا بنستخدمه كان **بيوصّل
للسيرفر ده بالظبط** — يعني كنا بنمشي في طريق طويل من غير داعي.

### قارنت بنفسي:

| | الوسيط | Moonton مباشرة |
|---|---|---|
| الأبطال | 131 | **133** (لقى Marcel و Hirara الجداد) |
| حد الطلبات | ~500/يوم | **مفيش** |
| السرعة | خطوتين | خطوة واحدة |
| لو وقع | كله يقف | عندنا 3 احتياطيات |

### اختبرته لايف دلوقتي:
```
✔ 133 بطل بصورهم
✔ Luo Yi: Mage · Mid Lane
✔ المهارات: Duality[Burst,CC] Dispersion[AOE] Rotation[AOE,Slow] Diversion[Teleport]
✔ كل أيقونات المهارات موجودة
✔ نسب حقيقية: Marcel W58.41% P0.22% B20.83%
✔ الكاونترات بالأسماء: Zhuxin +5.43%, Chip +5.26%
✔ رابط القصة الرسمي
```

### 4 طبقات حماية

```
1. Moonton مباشرة   ←  الأساسي (الأسرع والأكمل)
2. الـ API المجتمعي  ←  لو الأول وقع + الأيتمات
3. قاعدة البيانات    ←  آخر نسخة محفوظة
4. الملف المحلي      ←  البوت ما بيقعش أبداً
```

---

## 🧠 الـ AI بقى شايف كل ده

الكوتش كان بيشوف أرقام بس. دلوقتي بيشوف **كل حاجة**:

```
Fanny: winrate 42.07%, pickrate 0.63%, banrate 2.25%,
roles Assassin, lanes Jungle,
countered by Kaja, Khufra,
strong against Karrie, Irithel,
synergises with Minotaur, Gatotkaca
| skills: Air Superiority (Buff); Tornado Strike (AOE);
          Steel Cable (Mobility); Cut Throat (Burst/Mobility)
| why it loses: Fanny is countered by heroes that can restrict mobility,
  such as Kaja and Khufra, since they can easily shutdown her main advantage.
```

**ليه ده مهم؟** الموديلات بتخترع أسماء مهارات مش موجودة. دلوقتي الأسماء
الحقيقية قدامه، وضفت قاعدة صريحة: *«ممنوع تخترع اسم مهارة — لو مش في
البيانات، ما تسمّهاش»*.

**مش محتاج تضيف أي متغيّر** — كله بيشتغل تلقائي.

---

## 🐛 باج الرسالة المكسورة `{command}` `{given}`

الصورة اللي بعتها بتوضّح الباج:
```
❌ {command} has no subcommand called {given}.
Available: {options}
```

**السبب:** نظام الترجمة عندنا بيستبدل `{{متغيّر}}` بقوسين مزدوجين. أنا كتبت
الرسالة الجديدة بقوس واحد `{متغيّر}` — فما اتبدلتش وظهرت كنص خام.

**الحل:** صلّحتها، وكمان **فحصت كل ملفات الترجمة** (159 مفتاح × لغتين) للتأكد
إن مفيش رسالة تانية بنفس الغلط. النتيجة: **صفر**. وضفت اختبار يمنع تكرارها.

دلوقتي بتظهر كده:
```
⚠️ أمر فرعي غير معروف
❌ الأمر `hero` مفيهوش `meta`.
المتاح: info, counter, random

💡 `!meta` أمر مستقل — جرّبه كده.
```

### وكمان: `!hero meta` بقى يدلّك على `!meta`

`meta` **مش أمر فرعي** جوه `hero` — هو **أمر مستقل** بذاته. البوت بقى يفهم
ده ويقولك عليه:

| اللي تكتبه | البوت يقولك |
|---|---|
| `!hero meta` | `!meta` أمر مستقل |
| `!hero tierlist` | `!meta` (بيفهم الاختصارات كمان) |

### وكمان: شكل رسايل الخطأ اتظبط

الرسالة كانت متحطّة في **عنوان** الإمبد. العناوين في ديسكورد مش بتنزل سطر
جديد وبتبان كبيرة — علشان كده كان شكلها مكسور. نقلتها للوصف.

---

## 🎵 الموسيقى والقرآن — اختبرتهم دلوقتي

```
✔ Lavalink: serenetia-v4 (22 نتيجة بحث) · 5 نودات شغالة
✔ إذاعة القرآن: n0a.radiojar.com
✔ إذاعة القرآن: qurango.net/radio/tarateel
```

الاتنين شغالين. المشكلة عندك إنك **لسه على النسخة القديمة** اللي فيها:
- باج فحص Lavalink (كان بيرفض كل النودات الشغالة)
- باج الـ redirect (كان بيكسر البث والصلاة)

---

## 🖼️ الأيقونات والصور

الصور بتيجي من الـ API المباشر. **ملف البيانات المحلي مفيهوش صور** — فلو
الـ API مش شغال، الإمبد بيبان بسيط.

خليت البوت **يقولك ده بصراحة** بدل ما تحتار:
```
بيانات محلية — الصور تظهر لما يرجع الاتصال بالـ API
```

لو شفت الرسالة دي، يبقى الـ API مش واصل. اكتب `/doctor health` وشوف
`mlbbLive`.

---

## 📊 قراءة `/doctor health` بتاعك

اللي بعته فيه **3 حاجات**، وكلها اتصلحت:

### ✅ الشغال تمام
```
database ✅   locale ✅   http ✅   quran 4/4 ✅   jobs ✅   presence ✅
```
**الداتابيز شغالة!** فمفيش مشكلة فيها خالص.

### 🟡 1. `mlbbLive — All MLBB API endpoints failed (404)`

**السبب:** حطيت «مراية احتياطية» للـ API اسمها
`openmlbb.fastapicloud.dev`. التوثيق الرسمي للمشروع لسه بيقول إنها شغالة،
لكن اختبرتها دلوقتي وطلعت **ميتة تماماً** — بترجّع 404 حتى على صفحتها
الرئيسية:

```
https://openmlbb.fastapicloud.dev/           → 404
https://openmlbb.fastapicloud.dev/api        → 404
https://openmlbb.fastapicloud.dev/api/heroes → 404
```

فلما السيرفر الأساسي كان بيتأخر، البوت كان بيجرّب المراية الميتة ويرجّع
الخطأ بتاعها (404) — وده اللي ظهرلك.

**الحل:** شلت المراية. البوت دلوقتي يعتمد على `mlbb.rone.dev/api` بس، ولو
وقع بيرجع للبيانات المحلية على طول من غير ما يضيّع وقت.

> ملاحظة: `mlbbApi — serving 131 cached heroes` معناها إن النظام الاحتياطي
> اشتغل صح. البوت مكملش وقع، وده المفروض.

### 🟡 2. `music — LAVALINK_HOST not configured`

**ده كان باج في رسالة الحالة نفسها.** الكود كان بيسأل: «هل المستخدم كتب
`LAVALINK_HOST` بإيده؟» — وطبعاً لأ، لأن البوت **بيلاقي النود لوحده**.
فكان بيقول «مش متظبط» حتى لو الموسيقى شغالة فعلاً.

**الحل:** الحالة بقت بتتأكد من **النودات المتصلة فعلاً**، فهتقولك الحقيقة:
```
music — 5/5 nodes up · 0 active players ✅
```

### 🟢 3. `ai — openrouter/meta-llama/llama-3.3-70b-instruct:free ready`

مكتوب `ready` بس **الموديل ده اتشال من OpenRouter**. علشان كده `/coach`
بيفشل رغم إن المفتاح سليم. اتصلح في التحديث اللي فات (البوت بقى يكتشف
الموديلات المتاحة لوحده).

---

## ✅ بعد ما ترفع، المفروض تشوف

```
mlbbLive — live · 132 heroes ✅
music    — 5/5 nodes up ✅
ai       — openrouter/nvidia/nemotron-3-ultra-550b-a55b:free ready ✅
Modules  — 41/41 enabled
```

---

## 🔴 أهم حاجة: إنت لسه على نسخة قديمة

قلتلي إنك مرفعتش آخر حزمة. يعني **كل الإصلاحات اللي عملتها مش عندك أصلاً** —
الصور، الروابط، الأزرار، الـ API الجديد، إصلاح الموسيقى، إصلاح الأوامر.

**قبل أي حاجة تانية: نزّل `cc-immune-wispbyte.zip` وارفعه واعمل Restart.**

---

## 🕌 مشكلة `!prayer cairo egypt` — باج حقيقي واتصلح

**السبب:** موقع مواقيت الصلاة (Aladhan) بيعمل **تحويل تلقائي (302 redirect)**
لكل طلب. ومكتبة الشبكة اللي البوت بيستخدمها **مش بتتبع التحويل ده افتراضياً**،
فكانت بترجع رد **فاضي** — والبوت يقول «مقدرتش أجيب المواقيت».

اختبرته وأثبته:
```
من غير تتبّع التحويل:  302 · رد فاضي  ← الباج
مع تتبّع التحويل:      200 · Fajr 04:31, Dhuhr 13:01 ✅
```

**الحل:** فعّلت تتبّع التحويل (بحد أقصى 3 مرات عشان ما يعلّقش). ده كان بيأثر
على **كل** الخدمات الخارجية مش الصلاة بس.

اختبرته بعد الإصلاح:
```
cairo/egypt   → Fajr 04:31 · Dhuhr 13:01 · Asr 16:39 · Maghrib 19:52 · Isha 21:20 ✅
Giza/Egypt    → شغّال ✅
مدينة غلط     → رسالة خطأ واضحة ✅
```

---

## 🤖 مشكلة الـ AI — السبب اتحدد بالظبط

**المفتاح بتاعك سليم.** المشكلة إن **كل الموديلات المجانية اللي حطيتها
اتشالت من OpenRouter**. الموديلات المجانية بتتغيّر كل كام أسبوع.

فحصت الأربعة اللي كانوا مكتوبين:
```
✘ meta-llama/llama-3.3-70b-instruct:free   ← اتشال
✘ deepseek/deepseek-chat-v3-0324:free      ← اتشال
✘ google/gemma-3-27b-it:free               ← اتشال
✘ qwen/qwen-2.5-72b-instruct:free          ← اتشال
```

**الحل (عشان المشكلة ما تتكررش):** البوت بقى **يسأل OpenRouter عن الموديلات
المتاحة وقت الإقلاع** ويختار أحسنهم لوحده. مفيش أسماء ثابتة في الكود.

اختبرته دلوقتي:
```
Discovered 6 free OpenRouter model(s) · using nvidia/nemotron-3-ultra-550b-a55b:free
  1. nvidia/nemotron-3-ultra-550b-a55b:free   (مليون توكن!)
  2. inclusionai/ling-3.0-flash:free
  3. poolside/laguna-s-2.1:free
  ... و3 كمان كاحتياطي
```

**مش محتاج تعمل حاجة** — بس ارفع النسخة الجديدة. المفتاح اللي عندك هيشتغل.

---

## 🗄️ الداتابيز

قلتلي مش عارف الوضع. عملتلك دليل كامل بالصور والخطوات:

**👉 [`MONGODB-STEPS.md`](./MONGODB-STEPS.md)**

أهم نقطتين اللي بيقعوا فيهم الناس:
1. **Network Access** لازم يكون `0.0.0.0/0` (اسمح للكل) — Wispbyte مالوش IP ثابت
2. الباسورد **من غير رموز** زي `@` أو `#` أو `/` — بتكسر الرابط

الشكل الصح للرابط:
```
mongodb+srv://اليوزر:الباسورد@cluster0.xxxxx.mongodb.net/ccimmune?retryWrites=true&w=majority
                                                        ↑ اسم الداتابيز مهم
```

> بعد ما تظبطها اكتب `/doctor health` جوه ديسكورد — هيوريك حالة كل خدمة.

---

## 🚨 مشكلة «no longer exists» في أوامر كتير

### إيه اللي حصل بالظبط

لما كتبت `/radio` طلعتلك «الأمر مش موجود». بس `radio` **موجود فعلاً** — هو
أمر فرعي جوه `/quran`، يعني الصح `/quran radio`.

فالمشكلة **مشكلتين مع بعض**:

**أ) أوامر قديمة لسه مسجّلة على سيرفرك**
ديسكورد بيسجّل الأوامر على مستويين: **عام (global)** و**لكل سيرفر (guild)**.
البوت بينشر عام، لكن كان فيه أوامر قديمة مسجّلة **على السيرفر نفسه** من نشرة
قديمة. دي بتفضل ظاهرة في القايمة للأبد وبتغطّي على الأوامر الصح.

**ب) الإصلاح اللي عملته قبل كده كان ناقص** ⚠️
كتبت كود يمسح الأوامر القديمة، بس اشترط إنك تكتب `PRUNE_GUILD_ID` بإيدك.
إنت طبعاً مكتبتهوش، فالكود كان بيعدّي **من غير ما يمسح أي حاجة**. ده عيب في
شغلي أنا — الإصلاح اللي محتاج إعداد يدوي عشان يشتغل مش إصلاح.

### الحل دلوقتي

**1. البوت بقى يلاقي سيرفراتك لوحده** ويمسح الأوامر القديمة من غير ما تكتب
أي ID. اختبرته:
```
Removed 3 stale command(s) from guild 1529509091146338497: /radio, /join, /build
```

**2. ولو كتبت أمر فرعي بالغلط، البوت بقى يدلّك على الصح:**
```
ℹ️ مفيش أمر اسمه /radio لوحده.

اللي إنت عايزه:
/quran radio
```

بيشتغل مع كل الأوامر:

| اللي تكتبه | البوت يقولك |
|---|---|
| `/radio` | `/quran radio` |
| `/build` | `/item build` |
| `/ask` | `/coach ask` |
| `/reciter` | `/quran reciter` |
| `/nowplaying` | `/music nowplaying` |

### الأوامر الأساسية الصح

```
/hero info <البطل>        /quran radio         /coach ask <سؤال>
/hero counter <البطل>     /quran reciter       /coach draft
/item info <الأيتم>       /quran stop          /music nowplaying
/item build <البطل>       /prayer              /play <أغنية>
/meta                     /help                /setup auto
```

> 💡 **اكتب `/help` جوه السيرفر** — هيوريك كل الأوامر المتاحة بالترتيب.

---

## 🔧 إصلاحات الجولة دي (من اللوج اللي بعته)

### 1️⃣ `/coach` → «VAL-001 Invalid argument – Input rejected: empty»

**اللوج قال:** `mode=slash command=coach reason=empty`

**السبب الحقيقي:** لما تكتب `/coach` **من غير أمر فرعي**، ديسكورد بيسمح بده.
الكود كان مكتوب:
```js
const sub = ctx.sub() ?? 'ask';   // ← null يبقى 'ask'
```
فالبوت كان بيفكّر إنك طلبت `ask`، والسؤال فاضي طبعاً، فالـ AI Guard يرفضه
برسالة عن «مدخل فاضي» — رسالة مالهاش علاقة بمشكلتك الحقيقية.

**نفس الباج كان في 17 أمر** (`/config`, `/music`, `/ticket`, `/economy`...).

**الحل:** بدل ما أصلّح 17 ملف، حطيت **بوابة واحدة مركزية** في الراوتر: لو
الأمر ليه أوامر فرعية وإنت مكتبتش واحد، بيعرضلك **قائمة بيها كلهم** بدل
ما يشتغل غلط:
```
ℹ️ اختر أمراً فرعياً لـ /coach
  /coach ask      — Ask any MLBB question
  /coach analyze  — Analyse your performance
  /coach draft    — Get counter-pick suggestions
```

### 2️⃣ تحذير `ephemeral is deprecated`

ظهر في أول اللوج. discord.js v14 غيّر الطريقة. صلّحت كل الأماكن اللي كانت
بتستخدم الطريقة القديمة → **التحذير اختفى**.

### 3️⃣ الصور والروابط في الإمبد ✅

الصورة بتاعة Kaja اللي بعتها **من النسخة القديمة** (باين من `Cached` في
الفوتر ومفيش صور). النسخة الجديدة فيها:

| العنصر | إيه اللي بيظهر |
|---|---|
| صورة كبيرة | سبلاش آرت البطل بعرض الإمبد |
| صورة صغيرة | بورتريه البطل في الركن |
| تبويب المهارات | أيقونة المهارة نفسها |
| الأيتمات | صورة الأيتم الحقيقية |
| اللون | حسب دور البطل (Mage بنفسجي، Tank أزرق...) |

### 4️⃣ الروابط والربط بين المواضيع ✅

كل إمبد بطل فيه دلوقتي:
```
🔗 تفاصيل أكثر
📖 القصة الرسمية  •  📚 الويكي  •  🎬 شروحات وفيديوهات
```
الرابط الأول **رسمي من Moonton** (الـ API نفسه بيبعته).

وتحت الإمبد **صفّين أزرار**:
```
[📋 نظرة عامة] [✨ المهارات] [⚔️ المواجهات] [🎒 البيلد]
[🧠 اسأل الكوتش] [📖 صفحة البطل]
```

- **🎒 البيلد** — تبويب جديد بيوصّل البطل بالأيتمات المناسبة لدوره بالترتيب
- **🧠 اسأل الكوتش** — بيديك الأمر الجاهز منسوخ للبطل ده
- **📖 صفحة البطل** — زرار رابط مباشر للصفحة الرسمية

---

## ⚡ حل مشاكل الأوامر (الصور اللي بعتها)

الصور فيها **3 مشاكل مختلفة**، وكلها اتصلحت:

### 1️⃣ `/join` و `/build` → «The application did not respond»

**السبب:** الأمرين دول **مش موجودين في البوت أصلاً**. ديسكورد بيحتفظ
بالأوامر القديمة اللي اترفعت مرة، وبيفضل يعرضها في القايمة حتى لو اتشالت من
الكود. لما تضغط عليها، البوت مش لاقيها فكان **بيسكت** — وديسكورد بيعتبر
السكوت ده فشل ويقول «التطبيق مردّش».

> ملاحظة: الإمبد بتاع `Blade of Despair` في الصورة عليه **سعر ومفيش صورة** —
> ده معناه إن اللي شغال عندك لسه **النسخة القديمة**. لازم ترفع الجديدة.

**الحل (اتعمل في الكود):**
- البوت دلوقتي **بيرد فوراً** برسالة واضحة بدل ما يسكت
- الـ bootstrap بقى **يمسح الأوامر القديمة تلقائياً** أول ما ترفع

**عشان تمسحهم دلوقتي حالاً:** في الكونسول بتاع Wispbyte شغّل:
```
npm run undeploy
```
هيقولك بالظبط الأوامر اللي اتمسحت، وبعدين اعمل **Restart** وهيرفع الأوامر
الصح بس.

---

### 2️⃣ `!coach menu` → «That input is not valid» (VAL-001)

**السبب:** `menu` **مش subcommand موجود** في `/coach`. الموجود هو:
`ask` · `analyze` · `draft`.

بس البوت بدل ما يقولك كده، كان بيمرّر كلمة `menu` كإجابة للسؤال بتاع `ask`
وبيفشل برسالة غلط تماماً — رسالة عن «قيمة غير صالحة» مش عن «أمر فرعي غير
موجود».

**الحل:** ضفت كود خطأ جديد `VAL-003` بيقولك بالظبط:
```
❌ الأمر `coach` مفيهوش `menu`.
المتاح: ask, analyze, draft
```

**الصح تكتب:**
```
/coach ask السؤال بتاعك
/coach analyze
/coach draft
```

---

### 3️⃣ `/setup auto` — لسه محتاج تجربه

ده الأمر الصح والموجود فعلاً. لو مردّش، ابعتلي اللوج.

---

## ⚠️ الأول: افهم التحذيرات دي

التحذيرات في اللوج **مش أخطاء**. البوت شغال 100%:

```
✅ Logged in as CC Immune#0393
✅ Serving 1 guilds · 27 users
✅ /help executed successfully in 636ms
```

الرسايل الصفرا معناها: *"الخدمة دي محتاجة إعداد، وأنا قفلتها بدل ما أقع"*.
ده **تصميم مقصود** — البوت بيشتغل ناقص بدل ما يرفض يقوم.

| التحذير | معناه | إجباري؟ |
|---|---|---|
| `⊘ redis` | مفيش Redis → بيستخدم ذاكرة داخلية | ❌ لأ |
| `⊘ lavalink / music` | مفيش سيرفر صوت | ⚠️ لو عايز موسيقى |
| `ai-provider degraded` | مفيش مفتاح AI | ⚠️ لو عايز الكوتش |
| `2 degraded` | النتيجة النهائية للاتنين فوق | — |

---

## 1️⃣ المتغيّرات الأساسية (عندك بالفعل)

```
DISCORD_TOKEN      = <التوكن بتاعك>
CLIENT_ID          = <آي دي التطبيق>
MONGO_URI          = <رابط المونجو>
OWNER_IDS          = <الآي دي بتاعك>
NODE_ENV           = production
NODE_OPTIONS       = --max-old-space-size=384
CACHE_DRIVER       = memory
SHARDING_ENABLED   = false
API_SERVER_ENABLED = true
TIMEZONE           = Africa/Cairo
```

---

## 2️⃣ عشان تشيل تحذير الـ AI ✨
### (OpenRouter · مجاني · دقيقتين · بيفعّل `/coach` و`/translate`)

1. ادخل <https://openrouter.ai>
2. سجّل بحساب جوجل أو جيتهاب
3. **Keys** (أو مباشرة <https://openrouter.ai/keys>) → **Create Key**
4. انسخ المفتاح (بيبدأ بـ `sk-or-v1-...`)

```
OPENROUTER_API_KEY   = sk-or-v1-xxxxxxxxxxxxxxxx
AI_PROVIDER          = openrouter
AI_FALLBACK_PROVIDER = none
```

**بيتفعّل:** `/coach ask` · `/coach analyze` · `/coach draft` · `/translate` · ذاكرة الـ AI

### ليه OpenRouter؟
- **مفتاح واحد** يفتح لك عشرات الموديلات (Llama · DeepSeek · Gemma · Qwen · GPT-OSS)
- في موديلات **مجانية بالكامل** (اللي بينتهي اسمها بـ `:free`)
- **مش محتاج فيزا** ولا رصيد للموديلات المجانية
- لو عايز تغيّر الموديل، بتغيّر سطر واحد بس — من غير ما تغيّر المفتاح

### تغيير الموديل (اختياري)

```
OPENROUTER_MODEL = meta-llama/llama-3.3-70b-instruct:free
```

وده الافتراضي. لو عايز موديل تاني مجاني:

| الموديل | الـ slug | مناسب لإيه |
|---|---|---|
| Llama 3.3 70B | `meta-llama/llama-3.3-70b-instruct:free` | عام · افتراضي |
| DeepSeek V3 | `deepseek/deepseek-chat-v3-0324:free` | تحليل وتفكير أعمق |
| Gemma 3 27B | `google/gemma-3-27b-it:free` | سريع وخفيف |
| Qwen 2.5 72B | `qwen/qwen-2.5-72b-instruct:free` | عربي كويس |
| GPT-OSS 120B | `openai/gpt-oss-120b:free` | ردود أقوى |

> 📋 القايمة الكاملة والمحدّثة: <https://openrouter.ai/models?max_price=0>

### 🔁 سلسلة موديلات احتياطية (مفعّلة تلقائياً)

الموديلات المجانية بتتزحم وبيجيلها **rate limit**، وأحياناً بيتشالوا خالص.
عشان كده البوت عنده **سلسلة** بيمشي عليها لوحده:

```
OPENROUTER_FALLBACK_MODELS = deepseek/deepseek-chat-v3-0324:free,google/gemma-3-27b-it:free,qwen/qwen-2.5-72b-instruct:free
```

لو الموديل الأساسي رجّع `429` أو `404`، البوت يجرّب اللي بعده **من غير restart**
ومن غير ما المستخدم يحس بحاجة. لو المفتاح نفسه غلط (`401`) بيقف فوراً — عشان
ما يضيّعش وقت في محاولات فاشلة.

### حدود الـ free tier
- حوالي **20 طلب/دقيقة** · و**50 طلب/يوم** (بتبقى 1000/يوم لو حطيت 10$ رصيد مرة واحدة)
- كفاية جداً لسيرفر بـ 27 عضو
- البوت كمان عنده كوتة داخلية لكل مستخدم: `AI_DAILY_TOKEN_QUOTA=50000`

### احتياطي بمزوّد تاني (اختياري)

لو عايز أمان زيادة، خد مفتاح مجاني من <https://console.groq.com> وحط:

```
GROQ_API_KEY         = gsk_xxxxxxxx
AI_FALLBACK_PROVIDER = groq
```

كده لو OpenRouter كله وقع، البوت بينط على Groq تلقائياً.

---

## 3️⃣ إذاعة القرآن 📿
### ✅ شغالة لوحدها — من غير أي إعداد

البوت بيستخدم 4 مرايا للبث، ولو واحدة وقعت بينط للتانية:

```
✅ Quran mirrors: 4/4 live
```

**الأوامر:** `/quran radio` · `/quran reciter` · `/prayer`

مواقيت الصلاة متظبطة على مصر (`PRAYER_METHOD=5`). لو في بلد تانية:
```
PRAYER_DEFAULT_COUNTRY = Egypt
PRAYER_DEFAULT_CITY    = Cairo
```

> 🎵 **الموسيقى** ليها قسم لوحدها تحت (قسم 6) لأنها كان فيها باج واتصلح.

---

## 4️⃣ موقع البوت 🌐
### (مجاني · على نفس الاستضافة · مش محتاج دومين)

البوت بيقدّم **موقع كامل بثيم اللعبة** من نفس البورت. فيه تعريف بالبوت
ومميزاته وأرقام مباشرة وزراير الإضافة والسيرفر — بالعربي والإنجليزي.

```
API_SERVER_ENABLED = true
WEBSITE_ENABLED    = true
SUPPORT_SERVER_URL = https://discord.gg/xxxxxxx
```

**البورت بيتاخد لوحده** من `SERVER_PORT` بتاع Wispbyte — سيب `API_PORT` فاضي.

بعد الـ Restart هتلاقي في اللوج:
```
[Api] API server listening on 0.0.0.0:25591 · website enabled at /
```

افتح `http://<الآي بي بتاعك>:25591` — هتلاقيه في تبويب **Network** في اللوحة.

> 📖 الشرح الكامل (الدومين، الـ uptime monitor، حل المشاكل) في ملف
> [`WEBSITE.md`](./WEBSITE.md)

---

## 5️⃣ بيانات اللعبة والصور 🎮
### (اتصلحت بالكامل — مش محتاج تعمل حاجة)

**المشكلة اللي كانت:** الـ API القديم (`mlbb-stats.ridwaanhall.com`) **اتقفل
خلاص**، فكل الطلبات كانت بتفشل والبوت كان بيرجع لملف قديم فيه 131 بطل بس
**من غير أي صور**.

**الحل:** ربطته بالـ API الجديد الشغال، واللي بيجيب من Moonton نفسها:

| الحاجة | قبل | دلوقتي |
|---|---|---|
| الأبطال | 131 (ملف قديم) | **كل الروستر** مباشر من اللعبة |
| صور الأبطال | ❌ مفيش | ✅ سبلاش آرت + بورتريه + أيقونة |
| المهارات | ❌ مفيش | ✅ بأيقوناتها ووصفها وتاجاتها |
| الأيتمات | 45 من غير صور | **~150** بصورهم وإحصائياتهم |
| نسب الفوز | ثابتة في ملف | ✅ **مباشرة** من السيرفر |
| الكاونترات | قايمة مكتوبة يدوي | ✅ أرقام مواجهات حقيقية |
| العربي | ترجمة عندنا | ✅ **من Moonton نفسها** |

### الإمبد الجديد فيه إيه؟

```
🔮 Luo Yi                          [صورة البطل في الركن]
Mage · Mid Lane                    [أيقونة الدور]

🎭 الدور: 🔮 Mage    🛣️ المسار: 🎯 Mid Lane    ✨ التخصّص: Support

📈 إحصائيات مباشرة
   📈 نسبة الفوز: 52.4%
   📊 نسبة الاختيار: 3.1%
   🚫 نسبة الحظر: 12.8%

⚡ القدرات
   الضرر    ▰▰▱▱▱▱▱▱▱▱ 20
   التحمّل  ▰▰▰▰▰▰▰▰▰▰ 100
   التحكّم  ▰▰▰▰▱▱▱▱▱▱ 40

💪 قوي ضد: Eudora · Zilong     ⚠️ ضعيف أمام: Uranus · Fredrinn

           [ــــــ السبلاش آرت بعرض الإمبد ــــــ]

[📋 نظرة عامة] [✨ المهارات] [⚔️ المواجهات]   ← أزرار تفاعلية
```

اضغط على الأزرار تحت الإمبد يتبدّل المحتوى **في نفس الرسالة** — من غير ما
تكتب أمر تاني.

### التحديث التلقائي

البوت بيعيد سحب الأبطال والأيتمات **كل 6 ساعات** لوحده. لو نزل بطل جديد أو
اتغيّر أيتم، هيظهر عندك **من غير ما ترفع حاجة**.

المتغيّرات دي كلها ليها قيم افتراضية صح — **مش محتاج تضيفها**:
```
MLBB_API_BASE   = https://mlbb.rone.dev/api
MLBB_API_MIRROR = https://openmlbb.fastapicloud.dev/api   ← احتياطي تلقائي
MLBB_AUTO_SYNC  = true
```

---

## 6️⃣ الموسيقى 🎵 — اتصلحت
### كان فيه باج حقيقي

**السبب:** البوت كان بيفحص النودات بـ `/version` ويطلب إن الرد يبدأ بـ `4`.
لكن النودات الشغالة بترجّع حاجة زي `254a7d4ae788...-SNAPSHOT`، وبعضها بيرجّع
**404** على المسار ده أصلاً. فالبوت كان **بيرفض كل النودات الشغالة** ويقول
«الموسيقى مقفولة».

**الحل:** بقى يفحص `/v4/loadtracks` — ده بيثبت إن النود v4 **وإنه فعلاً
بيلاقي أغاني**، مش مجرد إنه بيرد.

اختبرته دلوقتي على الشبكة الحقيقية:
```
Probing 5 public Lavalink nodes…
Auto-discovered Lavalink: serenetia-v4 (global, 1948ms, search, 22 result(s))
                          · 4 backup(s) available
```

**5 نودات شغالة** — لو واحد وقع البوت بينط للتاني لوحده. مش محتاج تضيف أي
متغيّر.

---

## 7️⃣ Redis (اختياري — متجاهله)

```
REDIS_URL = redis://...
```

**مش محتاجه.** البوت بيستخدم ذاكرة داخلية وده كفاية تماماً لحد آلاف السيرفرات.
الفايدة الوحيدة لو شغّلت أكتر من نسخة من البوت مع بعض.

---

## 8️⃣ تحسينات مقترحة

```
DEFAULT_LOCALE = ar          ← البوت يرد بالعربي افتراضياً
DEFAULT_PREFIX = !           ← بادئة الأوامر النصية
DEV_GUILD_ID   = <آي دي سيرفرك>   ← الأوامر تظهر فوراً بدل ساعة
```

> `DEV_GUILD_ID` مفيد وإنت بتجرب. لما تخلص وتعمم البوت، **امسحه**
> عشان الأوامر تشتغل في كل السيرفرات.

---

## 📋 الجدول النهائي — كل حاجة شغالة

```
DISCORD_TOKEN        = <توكنك>
CLIENT_ID            = <آي دي التطبيق>
MONGO_URI            = <رابط المونجو>
OWNER_IDS            = <آي ديك>

NODE_ENV             = production
NODE_OPTIONS         = --max-old-space-size=384
CACHE_DRIVER         = memory
SHARDING_ENABLED     = false
API_SERVER_ENABLED   = true
TIMEZONE             = Africa/Cairo
DEFAULT_LOCALE       = ar

OPENROUTER_API_KEY   = <مفتاح OpenRouter>
AI_PROVIDER          = openrouter
AI_FALLBACK_PROVIDER = none

SUPPORT_SERVER_URL   = <لينك سيرفرك>
WEBSITE_ENABLED      = true

# الموسيقى بتشتغل لوحدها — مش محتاج تضيف حاجة
# البورت بيتاخد تلقائياً من SERVER_PORT — سيب API_PORT فاضي
```

بعد ما تضيفهم: **Restart**. المفروض تشوف:

```
[Bot] Modules: 40/40 enabled          ← بدل 33/40
[AI] AI provider: openrouter · model meta-llama/llama-3.3-70b-instruct:free (+3 model fallbacks)
[Music] Lavalink node "main" connected
[Quran] Quran mirrors: 4/4 live
[Api] API server listening on 0.0.0.0:25591 · website enabled at /
```

---

## 🤖 مشكلة "البوت ظاهر معلق"

**ده كان باج حقيقي وصلحته.**

السبب: البوت كان بيحط حالة `idle` وقت الإقلاع و**عمره ما بيغيّرها**.
فكان شكله نايم مع إنه بيرد على الأوامر عادي.

**الحل:** ضفت `PresenceService` بيحط الحالة `online` وبيدوّر على 6 حالات
بثيم اللعبة كل 45 ثانية:

```
🛡️ Immune to CC
⚔️ Watching 12 servers · 3,400 players
🔥 Competing in Meta: patch 1.9.44
🎯 Listening to /coach for AI analysis
📿 Listening to القرآن الكريم
🦸 Watching 25 heroes · /hero
```

> ⚠️ محتاج تحمّل الحزمة الجديدة عشان الإصلاح ده يوصلك.

---

## ✅ إزاي تتأكد إن كل حاجة تمام

في السيرفر اكتب:

```
/doctor health
```

هيوريك لوحة بحالة كل نظام. أو:

```
/module list
```

هيوريك الـ 40 نظام وحالة كل واحد.
