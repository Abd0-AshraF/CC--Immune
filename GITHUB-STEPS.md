# 📤 رفع البوت على GitHub — دليل مبسّط

> **ليه GitHub أحسن من رفع الملف؟**
> عشان لما تعدّل أي حاجة بعدين، بتضغط زرار واحد في Wispbyte وبيجيب التحديث لوحده.
> من غيره هتفضل ترفع ملف zip كل مرة.

---

## 🎯 الخلاصة في 3 سطور

1. تعمل حساب GitHub ومستودع (مكان تحط فيه الكود)
2. ترفع الكود عليه
3. تربط Wispbyte بالمستودع

**مش محتاج تعرف Git.** فيه طريقتين تحت، اختار اللي يريحك.

---

# الطريقة الأولى: من غير أوامر خالص 🖱️
### (الأسهل — بالسحب والإفلات)

## الخطوة 1: اعمل حساب GitHub

1. ادخل <https://github.com/signup>
2. اكتب إيميلك وباسورد واسم مستخدم
3. أكّد الإيميل

---

## الخطوة 2: اعمل مستودع (Repository)

1. بعد ما تدخل، اضغط علامة **➕** فوق على اليمين
2. اختار **New repository**
3. املأ كده:

| الحقل | اكتب إيه |
|---|---|
| **Repository name** | `cc-immune` |
| **Description** | (اختياري) `MLBB Discord Bot` |
| **Public / Private** | 🔴 اختار **Private** |
| **Add a README file** | ❌ **متعلّمش عليها** |
| **Add .gitignore** | ❌ **سيبها None** |

> 🔴 **ليه Private؟**
> عشان محدش يشوف كودك. لو حطيته Public وبالغلط رفعت التوكن،
> فيه بوتات بتفضل تدوّر على التوكنات في GitHub وتسرق بوتك في دقايق.
>
> ❌ **وليه متعلّمش على README و .gitignore؟**
> عشان المشروع أصلاً فيه الملفين دول، ولو GitHub عملهم هيحصل تعارض.

4. اضغط **Create repository**

---

## الخطوة 3: ارفع الملفات

دلوقتي هتلاقي صفحة فاضية مكتوب فيها كلام تقني. **متخافش منه.**

1. دوّر على السطر ده في نص الصفحة:
   > *uploading an existing file*

   اضغط على الكلمة دي (لينك أزرق).

2. **قبل ما ترفع — حاجة مهمة جداً:**

   افتح فولدر المشروع على جهازك، و**امسح دول لو موجودين**:
   - 🗑️ فولدر `node_modules` (لو موجود — حجمه كبير جداً)
   - 🗑️ ملف `.env` (لو موجود — **فيه التوكن بتاعك!**)

   > ✅ الملف اللي عملتهولك `cc-immune-wispbyte.zip` **أصلاً مفيهوش الاتنين دول**.
   > فكّه في فولدر وارفع اللي جواه.

3. **اسحب كل الملفات** من الفولدر وارميها في الصفحة

4. استنى لحد ما يخلّص الرفع (الشريط الأخضر يكمل)

5. تحت في خانة **Commit changes** اكتب: `أول رفع`

6. اضغط الزرار الأخضر **Commit changes**

✅ **خلاص! الكود بقى على GitHub.**

---

## الخطوة 4: اتأكد إن التوكن مترفعش 🔒

دي خطوة أمان **متعدّيهاش**:

1. في صفحة المستودع، بصّ على قايمة الملفات
2. دوّر على ملف اسمه **`.env`** (بنقطة في الأول)

| النتيجة | يعني إيه |
|---|---|
| ✅ **مش لاقيه** | تمام، إنت آمن |
| ❌ **لاقيه** | **خطر!** اعمل الآتي فوراً ⬇️ |

**لو لقيت `.env` مرفوع:**
1. ادخل Discord Developer Portal → Bot → **Reset Token** (التوكن القديم بقى محروق)
2. في GitHub: اضغط على ملف `.env` → أيقونة سلة المهملات 🗑️ → **Commit changes**
3. استخدم التوكن الجديد في Wispbyte بس

> ملف `.env.example` **عادي جداً** يكون موجود — ده نموذج فاضي من غير أسرار.

---

# الطريقة التانية: بالأوامر 💻
### (أسرع لو عندك Git متسطّب)

## سطّب Git الأول

- **ويندوز:** <https://git-scm.com/download/win> → ثبّت بالإعدادات الافتراضية
- **ماك:** افتح Terminal واكتب `git --version` وهيسألك يثبته

## بعدين

افتح Terminal (أو Git Bash على ويندوز) جوه فولدر المشروع واكتب الأوامر دي **واحد ورا التاني**:

```bash
# 1. جهّز المشروع للرفع
git init

# 2. عرّف نفسك (مرة واحدة بس في حياتك)
git config user.name "اسمك"
git config user.email "إيميلك@example.com"

# 3. ضيف كل الملفات
git add .

# 4. 🔴 اتأكد إن .env مش هيترفع — ده أهم سطر
git status | grep "\.env$"
```

> **لو الأمر ده مطلعش أي حاجة** ✅ ممتاز، كمّل.
> **لو طلع `.env`** ❌ قف! اكتب `git rm --cached .env` وبعدين كمّل.

```bash
# 5. احفظ نسخة
git commit -m "أول رفع"

# 6. اربط بالمستودع (بدّل USERNAME باسمك في GitHub)
git remote add origin https://github.com/USERNAME/cc-immune.git

# 7. ارفع
git branch -M main
git push -u origin main
```

**هيطلب منك تسجيل دخول:**
- **Username:** اسمك في GitHub
- **Password:** 🔴 **مش الباسورد العادي!** محتاج Token

**إزاي تجيب الـ Token:**
1. <https://github.com/settings/tokens>
2. **Generate new token** → **classic**
3. **Note:** اكتب `wispbyte`
4. **Expiration:** اختار `No expiration`
5. علّم على ✅ **repo** (الأولى في القايمة)
6. **Generate token** → **انسخه فوراً** (مش هيظهر تاني)
7. الصقه مكان الباسورد

---

# الخطوة الأخيرة: اربط Wispbyte 🔗

1. ادخل لوحة Wispbyte → السيرفر بتاعك
2. تبويب **Startup**
3. دوّر على إعدادات **Git** أو **Auto Update**
4. املأ:

| الحقل | القيمة |
|---|---|
| **Repository URL** | `https://github.com/USERNAME/cc-immune` |
| **Branch** | `main` |
| **Username** | اسمك في GitHub |
| **Token / Password** | الـ Token اللي عملته فوق |

> الـ Username والToken مطلوبين عشان المستودع Private.

5. اضغط **Save** وبعدين **Pull / Update**

**لو Wispbyte مفيهوش خيار Git:**
افتح **Console** واكتب:
```bash
git clone https://github.com/USERNAME/cc-immune.git .
```
(النقطة في الآخر مهمة — معناها "هنا")

---

# 🔄 لما تعدّل حاجة بعدين

## بالموقع:
1. ادخل المستودع → اضغط على الملف
2. أيقونة القلم ✏️ → عدّل
3. **Commit changes**
4. في Wispbyte: **Pull** بعدين **Restart**

## بالأوامر:
```bash
git add .
git commit -m "وصف التعديل"
git push
```
بعدين في Wispbyte: **Pull** → **Restart**

> ⚠️ لو التعديل ضاف أوامر جديدة، شغّل `npm run deploy` في الكونسول.

---

# ❓ مشاكل شائعة

| المشكلة | الحل |
|---|---|
| `remote origin already exists` | اكتب `git remote remove origin` وأعد الخطوة 6 |
| `Authentication failed` | استخدم **Token** مش الباسورد العادي |
| `failed to push some refs` | اكتب `git pull origin main --rebase` وبعدين `git push` |
| الرفع بطيء جداً | يبقى `node_modules` بيترفع — امسحه |
| Wispbyte مش شايف الكود | اتأكد إن `package.json` في **جذر** المستودع مش جوه فولدر |
| `Repository not found` | راجع اسم المستخدم، واتأكد إنك مدخّل الToken صح |

---

# ✅ تشيك ليست

- [ ] عملت حساب GitHub
- [ ] عملت مستودع **Private** اسمه `cc-immune`
- [ ] رفعت الملفات
- [ ] 🔒 **تأكدت إن `.env` مش موجود في المستودع**
- [ ] `package.json` ظاهر في الصفحة الرئيسية للمستودع
- [ ] ربطت Wispbyte بالمستودع
- [ ] حطيت المتغيّرات في لوحة Wispbyte (مش في GitHub!)

---

## 🔑 القاعدة الذهبية

> **الكود يروح GitHub · الأسرار تفضل في لوحة Wispbyte**

التوكن ومفتاح المونجو **مايتكتبوش في أي ملف بيترفع**.
بيتحطوا في تبويب **Startup → Variables** في Wispbyte بس.

---

بعد ما تخلص، كمّل من ملف **`WISPBYTE.md`** من قسم *"إعدادات السيرفر في اللوحة"*.
